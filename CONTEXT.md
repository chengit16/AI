# AI 智能平台领域语言

本文档统一平台核心业务概念，避免菜单可见性、页面注册和后端授权在不同模块中使用相互冲突的名称。

## 权限与资源

**权限（Permission）**：
以稳定 `permission_code` 表达的一项授权语义，描述对某类资源执行某个动作的许可范围。
_避免_：菜单权限、接口权限、按钮权限

**权限码（permission_code）**：
Permission 的稳定引用键，由页面、接口和策略共同使用；它不等同于路由、接口路径或角色名称。
_避免_：API Key、路由权限、角色编码

**页面资源（PageResource）**：
平台注册并允许菜单引用的页面能力；工作空间管理员不能通过菜单配置引入未注册页面。
_避免_：菜单页面、任意路由

**接口资源（ApiResource）**：
平台注册的 HTTP 操作及其访问级别；需要授权的接口必须引用唯一 Permission。
_避免_：接口地址、菜单接口

**菜单（Menu）**：
用于组织目录、页面入口和动作入口的导航节点；菜单可见性只影响体验，不构成后端授权事实。
_避免_：权限、路由表

**菜单接口绑定（MenuApiBinding）**：
平台注册动作菜单与 `ApiResource` 的冻结关联；两者必须使用同一 `permission_code`，工作空间配置不能创建或改写该关联。
_避免_：可编辑接口地址、前端请求配置

**工作空间菜单覆盖（WorkspaceMenuOverride）**：
某个个人或企业空间对已注册菜单的父目录、名称、图标、排序和可见性调整；未覆盖项继续使用平台注册值，覆盖不能引入任意路由、组件或接口。
_避免_：自定义页面、菜单发布快照

**角色菜单可见性（RoleMenuVisibility）**：
某个角色对已注册菜单入口的体验层裁剪事实；它不能授予 Permission，也不能替代后端策略决策。
_避免_：角色权限、接口授权

**资源注册表（ResourceRegistry）**：
平台当前可发布的 Permission、PageResource、ApiResource 和系统 Menu 的版本化集合。
_避免_：权限配置、菜单快照

**菜单发布（MenuRelease）**：
某个工作空间的版本化菜单治理记录，按草稿、校验、审批和发布状态转换；创建时冻结完整菜单快照，发布后不可改写。
_避免_：资源注册表、当前菜单配置

**菜单发布快照（MenuReleaseSnapshot）**：
创建菜单草稿时冻结的合并菜单、角色菜单可见性、菜单接口绑定、注册表版本和菜单版本；规范 JSON 的 SHA-256 摘要用于识别内容，后续配置变化不能改写快照。
_避免_：实时菜单配置、可编辑发布内容

**当前菜单发布（CurrentMenuRelease）**：
某个工作空间当前对使用者生效的已发布 MenuRelease 指针；普通活跃成员可以读取其消费快照，但发布历史和治理动作仍仅属于空间所有者。
_避免_：最新草稿、菜单配置版本

**菜单回滚发布（RollbackMenuRelease）**：
复制同一工作空间某个已发布快照而创建的新发布记录；回滚只移动当前发布指针，不覆盖或删除原历史。
_避免_：改写旧版本、数据库回退

**角色权限授权（RolePermissionGrant）**：
某个工作空间角色对一项 Permission 的持久化授权事实，同时声明该权限可作用的数据范围；角色身份本身不自动代表任何业务权限。
_避免_：角色权限字符串、前端权限列表

**数据范围（ResourceScope）**：
策略决策返回的可执行过滤范围，首期支持整个工作空间、部门子树、当前账号和明确资源集合；实际读取数据的 Repository 必须继续把它转换为查询或结果过滤条件。
_避免_：查询参数、前端筛选条件

**策略决策点（PolicyDecisionPoint）**：
统一合并 Permission 注册状态、可信主体、有效角色、RolePermissionGrant、Open API Key Scope 和资源属性并产生允许或拒绝结果的服务端边界；任何依赖不可用或结果不可验证时默认拒绝。
_避免_：页面权限判断、业务模块内置角色判断

**字段策略注册表（FieldPolicyRegistry）**：
版本化声明各资源字段的最低密级和是否继承资源密级，供策略决策点计算 `field_mask`；未注册资源不能通过统一数据出口暴露任意字段。
_避免_：前端字段配置、数据库列清单

**字段投影（FieldProjection）**：
数据责任模块依据 `field_mask` 在序列化响应、运行日志、检索结果和模型上下文形成前移除受限字段的统一出口操作；字段缺失表达不可见，不使用空字符串伪装授权。
_避免_：前端脱敏、模型自行隐藏

## 知识与文档

**知识库（KnowledgeBase）**：
归属于单个工作空间的知识组织边界，可声明部门范围并承载多个 Document；删除采用软删除，存在活跃文档时不能删除。
_避免_：对象存储目录、向量索引

**文档（Document）**：
KnowledgeBase 内一份可持续演进的逻辑资料，负责聚合多个不可变 DocumentVersion；删除文档不会改写历史版本。
_避免_：上传文件、文档版本

**文档版本（DocumentVersion）**：
Document 的一次不可变内容修订，按 `draft → ready → published → superseded` 单向转换；内容对象键和来源位置属于受限字段，不通过普通响应暴露。
_避免_：可覆盖文件、当前文档

**文档来源（DocumentSource）**：
描述某个 DocumentVersion 来自手工内容、上传、网页或未来 DataSource 的事实。首期仅记录来源类型和受保护定位信息，`data_source` 不代表已接入真实连接器。
_避免_：连接器实例、公开 URL

**当前文档发布（DocumentPublication）**：
某个 Document 当前生效的已发布版本指针；发布新版本会在同一事务中将旧版本标记为 `superseded` 并切换指针，不改写历史内容。
_避免_：最新草稿、覆盖旧版本

**受控上传（ControlledUpload）**：
由服务端有界读取、识别真实文件类型、执行失败关闭扫描并生成工作空间对象键的上传流程；只有扫描为 `clean` 的对象才能形成 DocumentSource，客户端不能声明对象键或扫描结果。
_避免_：客户端直传对象键、仅按扩展名校验

**工作空间对象（WorkspaceObject）**：
对象存储中归属于唯一工作空间的私有对象引用，键必须包含服务端生成的 `workspaces/{workspace_id}/` 前缀；业务和 HTTP 契约不暴露 Bucket 或对象键。
_避免_：公开文件 URL、用户文件路径

**上传安全事实（UploadSecurityFact）**：
与上传 DocumentSource 一起持久化的媒体类型、字节数、内容 SHA-256、扫描状态、扫描器版本和扫描时间；字段要么全部为空以兼容旧事实，要么作为完整 `clean` 结论同时存在。
_避免_：日志中的扫描文案、客户端声明类型

**入库任务（IngestionJob）**：
与 clean 上传事实同事务创建的持久化处理任务，通过租约认领、有限重试和稳定错误码表达解析生命周期；PostgreSQL 是任务状态与失败事实的唯一来源。
_避免_：Celery 消息状态、索引任务

**解析产物（ParsedArtifact）**：
对来源对象完成摘要复核、格式解析和 OCR 后生成的确定性 JSON，包含页面、Block、位置和 Parser 信息，不包含来源对象键；它是后续 Chunk 构建的输入，不代表索引已可用。
_避免_：Chunk 集合、原始文件副本

**中文 OCR 适配器（ChineseOcrAdapter）**：
隔离中文扫描件识别实现的可替换边界；当前由 Tika 与 Tesseract `chi_sim+eng` 提供实现，后续替换 PaddleOCR 不影响入库状态机或解析产物契约。
_避免_：固定 Tesseract 调用、多模态图片问答

**用量变更（UsageMutation）**：
身份模块向可信业务模块提供的原子配额接口；业务模块声明计量项、变化量和幂等键，由权益模块在共享事务中完成额度检查、计数、审计与 Outbox，不允许跨模块直接写权益私有表。
_避免_：直接更新计数器、业务模块自建配额

## 当前实施边界

阶段 1 已完成账号、企业治理、复杂组织、RBAC/ABAC、字段投影、菜单注册与发布、动态应用壳层、知识事实模型、对象上传安全及持久化入库任务（`P1D-03`）。上传事实与任务同事务创建，Worker 以租约和有限重试执行解析与中文 OCR，并产生不含来源对象键的确定性 Artifact。

下一节点为 `P1D-04` 权限元数据 Chunk、Embedding、关键词索引和索引版本原子切换。当前仍不扩展 SaaS、Go 运行层、真实多源连接器、LLM Grading、多模态图片问答、Channel Gateway 或 Durable Run。
