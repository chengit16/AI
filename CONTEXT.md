# AI 智能平台领域语言

本文档统一平台核心业务概念，避免菜单可见性、页面注册和后端授权在不同模块中使用相互冲突的名称。

## 权限与资源

**权限（Permission）**：
以稳定 `permission_code` 表达的一项授权语义，描述对某类资源执行某个动作的许可范围。
_避免_：菜单权限、接口权限、按钮权限

**权限码（permission_code）**：
Permission 的稳定引用键，由页面、接口和策略共同使用；它不等同于路由、接口路径或角色名称。
_避免_：API Key、路由权限、角色编码

**页面资源（PageResource）**：
平台注册并允许菜单引用的页面能力；工作空间管理员不能通过菜单配置引入未注册页面。
_避免_：菜单页面、任意路由

**接口资源（ApiResource）**：
平台注册的 HTTP 操作及其访问级别；需要授权的接口必须引用唯一 Permission。
_避免_：接口地址、菜单接口

**菜单（Menu）**：
用于组织目录、页面入口和动作入口的导航节点；菜单可见性只影响体验，不构成后端授权事实。
_避免_：权限、路由表

**菜单接口绑定（MenuApiBinding）**：
平台注册动作菜单与 `ApiResource` 的冻结关联；两者必须使用同一 `permission_code`，工作空间配置不能创建或改写该关联。
_避免_：可编辑接口地址、前端请求配置

**工作空间菜单覆盖（WorkspaceMenuOverride）**：
某个个人或企业空间对已注册菜单的父目录、名称、图标、排序和可见性调整；未覆盖项继续使用平台注册值，覆盖不能引入任意路由、组件或接口。
_避免_：自定义页面、菜单发布快照

**角色菜单可见性（RoleMenuVisibility）**：
某个角色对已注册菜单入口的体验层裁剪事实；它不能授予 Permission，也不能替代后端策略决策。
_避免_：角色权限、接口授权

**资源注册表（ResourceRegistry）**：
平台当前可发布的 Permission、PageResource、ApiResource 和系统 Menu 的版本化集合。
_避免_：权限配置、菜单快照

**菜单发布（MenuRelease）**：
某个工作空间的版本化菜单治理记录，按草稿、校验、审批和发布状态转换；创建时冻结完整菜单快照，发布后不可改写。
_避免_：资源注册表、当前菜单配置

**菜单发布快照（MenuReleaseSnapshot）**：
创建菜单草稿时冻结的合并菜单、角色菜单可见性、菜单接口绑定、注册表版本和菜单版本；规范 JSON 的 SHA-256 摘要用于识别内容，后续配置变化不能改写快照。
_避免_：实时菜单配置、可编辑发布内容

**当前菜单发布（CurrentMenuRelease）**：
某个工作空间当前对使用者生效的已发布 MenuRelease 指针；普通活跃成员可以读取其消费快照，但发布历史和治理动作仍仅属于空间所有者。
_避免_：最新草稿、菜单配置版本

**菜单回滚发布（RollbackMenuRelease）**：
复制同一工作空间某个已发布快照而创建的新发布记录；回滚只移动当前发布指针，不覆盖或删除原历史。
_避免_：改写旧版本、数据库回退

**角色权限授权（RolePermissionGrant）**：
某个工作空间角色对一项 Permission 的持久化授权事实，同时声明该权限可作用的数据范围；角色身份本身不自动代表任何业务权限。
_避免_：角色权限字符串、前端权限列表

**数据范围（ResourceScope）**：
策略决策返回的可执行过滤范围，首期支持整个工作空间、部门子树、当前账号和明确资源集合；实际读取数据的 Repository 必须继续把它转换为查询或结果过滤条件。
_避免_：查询参数、前端筛选条件

**策略决策点（PolicyDecisionPoint）**：
统一合并 Permission 注册状态、可信主体、有效角色、RolePermissionGrant、Open API Key Scope 和资源属性并产生允许或拒绝结果的服务端边界；任何依赖不可用或结果不可验证时默认拒绝。
_避免_：页面权限判断、业务模块内置角色判断

**字段策略注册表（FieldPolicyRegistry）**：
版本化声明各资源字段的最低密级和是否继承资源密级，供策略决策点计算 `field_mask`；未注册资源不能通过统一数据出口暴露任意字段。
_避免_：前端字段配置、数据库列清单

**字段投影（FieldProjection）**：
数据责任模块依据 `field_mask` 在序列化响应、运行日志、检索结果和模型上下文形成前移除受限字段的统一出口操作；字段缺失表达不可见，不使用空字符串伪装授权。
_避免_：前端脱敏、模型自行隐藏

## 知识与文档

**知识库（KnowledgeBase）**：
归属于单个工作空间的知识组织边界，可声明部门范围并承载多个 Document；删除采用软删除，存在活跃文档时不能删除。
_避免_：对象存储目录、向量索引

**知识文件夹（KnowledgeFolder）**：
归属于单个工作空间的树形内容组织节点；它只确定 Document 的主目录，不代表检索边界、权限授权或对象存储路径。
_避免_：知识库、物理目录、团队空间

**默认根目录（DefaultKnowledgeFolder）**：
每个工作空间唯一且不可删除的 KnowledgeFolder 根节点，用于承接存量文档和原目录不可用时恢复的文档。
_避免_：默认知识库、回收站

**知识标签（KnowledgeTag）**：
工作空间内可复用并以多对多关系标记 Document 的分类词；标签不授予权限，也不扩大检索范围。
_避免_：权限标签、企业分类

**文档收藏（DocumentFavorite）**：
账号在指定工作空间内对 Document 的私有偏好关系，只影响该账号的快捷视图，不改变文档归属或其他成员视图。
_避免_：置顶文档、共享收藏夹

**文档回收站（DocumentTrash）**：
Document 软删除后的生命周期视图，保留原文件夹和清理到期事实；它不是可独立放置文档的文件夹或第二份文档副本。
_避免_：回收站文件夹、归档库

**企业分类（EnterpriseCategory）**：
企业工作空间中的知识治理分类，可关联发布审批和访问策略；它通过关系引用 Document，不拥有或复制文档内容。
_避免_：文件夹、知识库、权限角色

**团队知识域（TeamKnowledgeDomain）**：
企业工作空间内面向成员或部门的协作与 RAG 边界，声明知识库集合、主体范围和策略版本；实际可访问范围始终与当前 PDP 授权取交集。
_避免_：团队文件夹、企业知识库副本、全企业检索

**文档（Document）**：
KnowledgeBase 内一份可持续演进的逻辑资料，负责聚合多个不可变 DocumentVersion；删除文档不会改写历史版本。
_避免_：上传文件、文档版本

**文档版本（DocumentVersion）**：
Document 的一次不可变内容修订，按 `draft → ready → published → superseded` 单向转换；内容对象键和来源位置属于受限字段，不通过普通响应暴露。
_避免_：可覆盖文件、当前文档

**文档来源（DocumentSource）**：
描述某个 DocumentVersion 来自手工内容、上传、网页或未来 DataSource 的事实。首期仅记录来源类型和受保护定位信息，`data_source` 不代表已接入真实连接器。
_避免_：连接器实例、公开 URL

**当前文档发布（DocumentPublication）**：
某个 Document 当前生效的已发布版本指针；发布新版本会在同一事务中将旧版本标记为 `superseded` 并切换指针，不改写历史内容。
_避免_：最新草稿、覆盖旧版本

**文档发布请求（DocumentPublishRequest）**：
企业空间中针对一个不可变 DocumentVersion 发起的发布审批事实；批准只允许知识模块在重新鉴权后切换 DocumentPublication，驳回、撤回或过期不删除该版本。
_避免_：上传审批、文档版本、审批实例

**受控上传（ControlledUpload）**：
由服务端有界读取、识别真实文件类型、执行失败关闭扫描并生成工作空间对象键的上传流程；只有扫描为 `clean` 的对象才能形成 DocumentSource，客户端不能声明对象键或扫描结果。
_避免_：客户端直传对象键、仅按扩展名校验

**工作空间对象（WorkspaceObject）**：
对象存储中归属于唯一工作空间的私有对象引用，键必须包含服务端生成的 `workspaces/{workspace_id}/` 前缀；业务和 HTTP 契约不暴露 Bucket 或对象键。
_避免_：公开文件 URL、用户文件路径

**上传安全事实（UploadSecurityFact）**：
与上传 DocumentSource 一起持久化的媒体类型、字节数、内容 SHA-256、扫描状态、扫描器版本和扫描时间；字段要么全部为空以兼容旧事实，要么作为完整 `clean` 结论同时存在。
_避免_：日志中的扫描文案、客户端声明类型

**入库任务（IngestionJob）**：
与 clean 上传事实同事务创建的持久化处理任务，通过租约认领、有限重试和稳定错误码表达解析生命周期；PostgreSQL 是任务状态与失败事实的唯一来源。
_避免_：Celery 消息状态、索引任务

**解析产物（ParsedArtifact）**：
对来源对象完成摘要复核、格式解析和 OCR 后生成的确定性 JSON，包含页面、Block、位置和 Parser 信息，不包含来源对象键；它是后续 Chunk 构建的输入，不代表索引已可用。
_避免_：Chunk 集合、原始文件副本

**中文 OCR 适配器（ChineseOcrAdapter）**：
隔离中文扫描件识别实现的可替换边界；当前由 Tika 与 Tesseract `chi_sim+eng` 提供实现，后续替换 PaddleOCR 不影响入库状态机或解析产物契约。
_避免_：固定 Tesseract 调用、多模态图片问答

**索引版本（IndexVersion）**：
对一个已成功解析的 DocumentVersion 执行一次不可变索引构建的持久化事实，冻结 Artifact 摘要、Chunker、Tokenizer、Embedding 模型和权限元数据；重建产生递增 `build_no`，不覆盖历史构建。
_避免_：文档版本、Celery 任务、当前索引

**可追溯 Chunk（TraceableChunk）**：
由固定解析产物生成的最小检索单元，携带文档版本、入库任务、来源、Parser、OCR、权限范围和内容摘要；构建完成不代表可见，只有当前发布索引内的 Chunk 才能激活。
_避免_：解析 Block、无权限元数据的文本片段

**当前文档索引（CurrentDocumentIndex）**：
某个 Document 当前允许检索的 IndexVersion 指针；文档发布、索引完成、重建和撤权通过数据库事务切换它，并同时更新 Chunk 可见性，任何时刻最多一个索引版本处于活动状态。
_避免_：最新构建、文档发布指针

**Embedding 适配器（EmbeddingAdapter）**：
隔离文本向量实现、模型版本和维度契约的可替换边界；本地 `deterministic-hash-1024-v1` 只验证功能闭环，不代表真实语义质量。
_避免_：固定供应商 SDK、质量评估结论

**用量变更（UsageMutation）**：
身份模块向可信业务模块提供的原子配额接口；业务模块声明计量项、变化量和幂等键，由权益模块在共享事务中完成额度检查、计数、审计与 Outbox，不允许跨模块直接写权益私有表。
_避免_：直接更新计数器、业务模块自建配额

## Agent 服务调用

**服务调用（ServiceInvocation）**：
通过自定义知识 Agent、场景应用或 Open API 出口对一个已发布 Service 发起的受控执行；每次调用绑定唯一 Run、Route 和 AgentRelease，且不进入普通私有会话列表。
_避免_：普通问答会话、外部任务

**调用 Actor（InvocationActor）**：
拥有服务调用幂等、读取、审计和运行归属的请求身份；浏览器调用时等于账号，Open API 调用时是独立于创建账号的 API Key Actor。
_避免_：API Key 创建账号、服务账号

**服务级 Scope（ServiceResourceScope）**：
以 `service.definition.read.{service_id_hex}` 精确收窄 Open API Key 可调用 Service 的凭证范围；它不能授予账号原本没有的 RBAC/ABAC 权限。
_避免_：服务访问策略、文档权限

## 工具执行

**工具定义版本（ToolDefinitionVersion）**：
一个不可变工具版本，固定工具键、参数与结果 Schema 摘要、读写类型、风险、权限、Adapter 类型、超时、重试和凭证需求；被 Release 或 Run 引用后不能原地修改或删除。
_避免_：模型函数描述、可变工具配置

**工作空间工具目录（WorkspaceToolCatalog）**：
按平台注册的最新活动 ToolDefinitionVersion、当前活动套餐允许版本和当前 PDP Permission 结果取交集形成的可用工具视图；它只会收窄平台能力，不能通过菜单、草稿或请求参数扩大权限，历史 Release 仍按精确工具版本校验。
_避免_：菜单工具列表、模型自报工具、套餐直接授权

**工具任务（ToolRun）**：
一次绑定可信工作空间、调用主体、Service、AgentRelease 和预算的受控工具执行；它聚合有序 ToolStep，并拥有取消、超时和最终状态事实。
_避免_：模型对话、Durable Run、队列消息

**工具步骤（ToolStep）**：
ToolRun 中一个冻结顺序、工具版本和规范参数摘要的执行单元；每次执行前重新校验 Release 允许列表、当前策略、确认和预算。
_避免_：模型候选意图、可变执行计划

**工具尝试（ToolAttempt）**：
ToolStep 的一次追加式租约执行事实，具有稳定尝试序号与租约代际；取消、超时或租约失效后不能创建新尝试，迟到结果只作为审计证据。
_避免_：覆盖重试计数、Worker 进程状态

**工具调用（ToolCall）**：
ToolAttempt 中对一个不可变 ToolDefinitionVersion 的确定性调用事实；模型只能提出候选意图，Runtime 负责参数校验、策略、确认、凭证注入、Adapter 调用和结果安全。
_避免_：模型直接调用、任意 HTTP 请求

**工具确认（ToolConfirmation）**：
副作用调用执行前的个人所有者确认或企业多级审批事实，绑定工作空间、Run、Step、工具版本、规范参数摘要和策略版本；参数或策略变化、撤权、过期和撤回都会使其失效。
_避免_：页面弹窗状态、通用二次确认

**工具幂等记录（ToolIdempotencyRecord）**：
副作用发生前持久化的稳定调用身份与请求摘要；同键同请求返回既有结果，同键异请求拒绝，结果无法确定时进入人工对账而不自动重放。
_避免_：HTTP 请求 ID、Worker 去重缓存

**工具安全结果（ToolSafeResult）**：
通过 Schema、大小、敏感字段和 Prompt Injection 检查后形成的最小结果事实；只有检查全部通过的结果才能进入后续模型上下文，契约和观测只保存摘要而非正文。
_避免_：Adapter 原始响应、模型自行过滤

## 当前实施边界

阶段 1～4 已完成并冻结个人/企业工作空间、组织权限、知识问答、工作流审批、可靠性、Agent 发布与受控工具执行底座。阶段 5 的本地质量、成本、隔离和合规机制大部分已经完成，真实模型质量与真实价格输入仍受外部条件阻塞，不能用合成数据替代。

阶段 6 已按 `P6A-01` 启动第一版产品化改造，范围固定为阶段 A“知识组织与个人工作台”和阶段 B“企业知识门户与治理体验”。当前只冻结 KnowledgeFolder、KnowledgeTag、DocumentFavorite、DocumentTrash、EnterpriseCategory、TeamKnowledgeDomain 和 DocumentPublishRequest 的领域关系，尚未实现其数据库、API 或页面。

第一版不建设在线正文、富文本编辑、自动保存、文档 AI 写回或多人实时协作；公开助手、知识广场、标准 MCP 出口和支付同样保持独立后置。AI 助手与 AI 企业大脑可以进行本地机制和合成数据验证，对外开通仍受阶段 5 的真实质量、成本、隔离和数据政策门禁约束。
