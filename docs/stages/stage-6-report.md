# 阶段 6 验证报告

## 1. 当前结论

| 项目 | 当前值 |
| --- | --- |
| 阶段状态 | 进行中 |
| 当前节点 | `P6B-05` 权限管理与审计页（已验证，待提交） |
| 第一版范围 | 阶段 A+B |
| 在线文档编辑器 | `POST-EDITOR-01`，第一版不实施 |
| 阶段 5 关系 | 非模型节点并行建设；AI 对外开通门禁不变 |
| 阶段结论 | 阶段 A 与 `P6B-01`～`P6B-04` 已提交完成；`P6B-05` 已验证待提交，之后继续 `P6B-06`～`P6B-07` |

## 2. 入口条件审计

- 阶段 1～4 已完成，本阶段复用身份、工作空间、组织、RBAC/ABAC、知识、解析索引、检索、审批、Agent、Worker、审计和数据生命周期底座。
- 阶段 5 的真实质量和真实价格仍受外部输入阻塞；该阻塞不被阶段 6 页面或合成数据结果覆盖。
- 参考站个人/企业功能和数据流已经完成有数据调研，差距基线见 [功能差距分析](../product/jitknow-feature-gap-analysis.md)。
- 当前仓库代码核对确认 `knowledge` 模块拥有 `KnowledgeBase`、`Document`、`DocumentVersion`、`DocumentSource` 与 `DocumentPublication`；`workflow` 拥有审批策略与 Runtime；新组织关系不建立第二份内容事实。

## 3. `P6A-01` 验证记录

### 3.1 交付物

- [阶段 6 第一版范围与信息架构基线](../product/stage-6-v1-scope-and-information-architecture.md)：冻结 D1～D14、页面动作、状态、流程和迁移规则。
- [ADR-016](../decisions/ADR-016-knowledge-organization-and-governance-boundaries.md)：固定唯一内容事实、组织关系、发布审批对象和回滚边界。
- [阶段 6 实施计划](./stage-6-plan.md)：建立 `P6A-01`～`P6B-07` 节点、依赖和门禁。
- [领域语言](../../CONTEXT.md)：补充文件夹、标签、收藏、回收站、企业分类、团队知识域和发布请求术语。
- 主设计、改造计划、交付规范和项目进度看板同步为阶段 6 正式启动状态。

### 3.2 场景审计

| 场景 | 预期结论 | 当前结果 |
| --- | --- | --- |
| 文件夹移动文档 | 只改变组织关系，不复制版本/对象/Chunk | `P6A-01` 设计通过；组织关系实现见 4.1，文件详情体验继续由 `P6A-03` 验收 |
| 标签删除 | 只删除绑定，不删除文档 | `P6A-01` 设计通过；实现与当前验证见 4.1～4.3 |
| 原文件夹已删除后恢复文档 | 回到默认根目录 | `P6A-01` 设计通过；实现与当前验证见 4.1～4.3 |
| 企业版本提交审批 | 上传解析不受阻，只有发布指针切换待审批 | `P6B-04` 已实现并通过自动化与页面验证 |
| 审批期间成员撤权 | 批准后发布前重新鉴权，失败不切换 | `P6B-04` 已实现；撤权与治理漂移由后端专项验证 |
| AI 企业大脑范围为空 | 返回空结果，不回退到全企业 | 设计通过，代码待 `P6B-06` |
| 跨空间关系或检索命中 | 阻断整次操作并告警 | 设计通过，代码与专项测试待后续节点 |
| 新建在线文档 | 第一版不存在入口和正文模型 | 范围通过 |

### 3.3 验证命令

`P6A-01` 为文档、范围和领域边界节点，已执行与风险匹配的定向门禁：

```bash
git diff --check
python scripts/check_repository_policy.py
rg -n "Codex调研-[0-9]" docs/product docs/stages docs/decisions CONTEXT.md ai-intelligent-platform-v2-design.md docs/project-progress.md
```

定向门禁结果：`git diff --check` 通过，仓库敏感信息扫描通过，未发现被禁止的“Codex调研-日期时间”数据前缀；文档中的相对链接和第一版禁入能力边界已人工核对。

同时尝试执行完整 `./scripts/verify`。前端格式、Lint、类型检查、构建和 `63/63` 个前端测试通过，Python 格式、Lint、mypy 和非基础设施测试通过；由于本机 PostgreSQL 未监听 `127.0.0.1:5432`，以及 Tika/MinIO 依赖未启动，整体验证报告 `229` 个 PostgreSQL 集成错误和 `8` 个外部服务失败，命令以非零状态结束。这些失败不涉及本节点新增代码，但 `P6A-02` 起必须在真实依赖可用后补做对应集成门禁。

## 4. `P6A-02` 验证记录

### 4.1 已实现范围

- Revision `20260823_0072` 建立 `knowledge_folders`、`knowledge_tags`、文档目录/标签绑定和成员收藏表；为每个存量空间创建确定性默认根目录，并将活动及回收站文档补入唯一主目录。生命周期表注册表同步升至 `v17`，五张新增工作空间表纳入导出、清除和覆盖检查。
- Knowledge 模块在同一事务中完成目录、标签、收藏、软删除、恢复、永久删除、审计和 Outbox；所有接口从可信 `RequestContext` 取得 `workspace_id`，目录/标签写入要求 Owner，文档关系继续执行资源级授权。
- 文档摘要新增当前目录、活动标签和当前账号收藏投影；查询使用相关子查询避免标签连接放大，并继续执行知识库、活动状态、工作空间和资源授权交集。
- 回收站恢复在原目录不可用时回落到确定性默认根目录；普通主目录解绑同样采用替换语义回落默认根目录，默认根目录自身不能解绑。
- 手工永久删除与保留期清理都在删除数据库事实前收集上传原件、解析产物和索引引用对象键，并统一发出 `knowledge.document.trash_purge_requested`；数据库删除与 Outbox 意图保持同一事务。
- 解析产物键由 API 与 Worker 共享规则根据工作空间、版本和任务身份确定；即使 Worker 尚未回写对象键，清理意图仍包含预期产物。永久删除拒绝运行中租约，保留期跳过运行中文档，失租 Worker 补偿删除自己刚写出的产物。
- Worker 新增有限批次、带时区截止时间、`SKIP LOCKED` 和可选工作空间边界的保留期任务；对象清理消费者严格验证事件删除事实、当前 `workspace_id` 前缀和规范对象键，任一键越界时整次失败关闭。
- 入库 Worker 领取任务时按 `workspace_id + document_id` 显式关联活动文档；回收站任务不能因同空间存在其他活动文档而被错误领取，已持有租约继续按失租规则收敛。
- 统一授权入口把目录和标签路径中的 `folder_id`、`tag_id` 冻结为 PDP 资源标识和属性；资源级策略与审计不再误用工作空间 ID。
- MinIO Adapter 复用 S3 删除不存在对象的幂等语义；对象存储或消费回执数据库暂时故障最多重试 3 次，只有全部对象删除成功后才提交专用消费回执，不为已删除文档重建通用资源投影。
- 知识页面新增知识库选择、目录树、目录创建/改名/移动/删除、标签管理、名称和标签筛选、单篇/批量移动与标签、收藏、文档/目录回收站以及桌面/移动布局；原上传、新版本、解析、发布和重试入口继续保留。
- OpenAPI 新增字段采用带默认值的 v1 兼容扩展，前端 Service 层归一化为页面确定结构；契约兼容检查相对 `HEAD` 通过。
- 文件夹 RAG 开关不进入本节点。`ADR-016` 明确 `KnowledgeBase` 是检索边界，文件夹只负责组织内容，不提供不影响真实检索的假开关。

### 4.2 已执行验证

```bash
pnpm --filter @ai-platform/web test
MYPYPATH=apps/api/src:apps/worker/src:packages/backend/src:packages/contracts/src \
  .venv/bin/pytest -q tests/unit/test_p6a02_knowledge_organization.py \
  apps/worker/tests/test_trash_retention.py apps/worker/tests/test_object_cleanup.py
MYPYPATH=apps/api/src:apps/worker/src:packages/backend/src:packages/contracts/src \
  .venv/bin/pytest -q tests/unit/test_p6a02_knowledge_organization.py \
  tests/unit/test_p1c01_resource_registry.py tests/unit/test_p1d03_ingestion_jobs.py \
  apps/worker/tests
MYPYPATH=apps/api/src:apps/worker/src:packages/backend/src:packages/contracts/src \
  .venv/bin/pytest -q apps/api/tests/test_authorization_api.py
.venv/bin/python scripts/check_contract_compatibility.py HEAD
AI_PLATFORM_TEST_DATABASE_URL='postgresql+psycopg://ai_platform:local-development-only@127.0.0.1:5432/ai_platform' \
  AI_PLATFORM_TIKA_URL='http://127.0.0.1:9998' \
  AI_PLATFORM_MINIO_ENDPOINT='http://127.0.0.1:9000' ./scripts/verify
./platform status
```

验证结果：前端 `22` 个测试文件、`69/69` 个测试通过；P6A-02 组织、保留期和对象清理定向测试 `26/26` 通过，Worker、资源注册表与入库失租补偿扩大回归 `46/46` 通过，授权 API `7/7` 通过。真实 PostgreSQL 上的知识组织 `5/5`、入库 Attempt `4/4`、空库/存量库 Migration 往返 `8/8` 通过；Tika 真实解析链路 `9/9`、MinIO/对象清理/回收期 `18/18` 通过。生命周期 Registry 升至 `v17` 后，新增五表的导出、清除、运营菜单升级与法规治理联合回归 `15/15` 通过。

公共开发数据库已从 Revision `20260820_0071` 迁移到 `20260823_0072`，PostgreSQL、Valkey、MinIO、Tika、API 和 Web 均可访问。2026-08-25 在最终修复与文档同步后再次执行完整 `./scripts/verify`，React `69/69`、Python `971/971`、Ruff、mypy strict（`773` 个源文件）、契约兼容、资源注册表、架构、注释、UnoCSS、供应链和生产构建全部通过。

### 4.3 浏览器与真实数据流验证

2026-08-24～2026-08-25 使用本地合成账号和个人工作空间执行真实浏览器点击，未使用 Mock API 替代页面数据流。验证覆盖知识库选择、目录创建/改名/移动/软删除/恢复/永久删除、标签创建/改名/绑定/删除/恢复、文档单篇与批量移动/标签、收藏切换、文档删除/恢复/永久删除和 Outbox 人工重放。

| 场景 | 实际结果 |
| --- | --- |
| 目录组织 | 创建、改名、层级移动、文档移动、软删除、恢复和永久删除均成功；默认根目录继续存在，文档与版本/对象/Chunk 没有因目录操作被复制 |
| 标签生命周期 | 文档绑定数为 `1` 时删除标签，绑定数立即变为 `0`，文档仍存在；恢复标签后旧绑定保持为空，避免已删除关系被隐式复活 |
| 收藏 | 同一文档收藏与取消收藏均成功，列表计数和当前账号投影同步变化 |
| 文档回收站 | 软删除后回收站计数从 `0` 变为 `1`；恢复后返回活动列表；再次删除并永久删除时接口返回真实 `204 No Content`，页面提示“文档已永久删除”，回收站计数恢复为 `0` |
| 数据库事实 | 永久删除后对应文档行数为 `0`；数据库删除与 `knowledge.document.trash_purge_requested` Outbox 意图处于同一事务结果中 |
| 外部对象清理 | 事件 `6f7dfaa6-dbdc-4fd0-b0a5-bda3c9a7279e` 以原因码 `WORKER_RUNTIME_REFRESH` 重放后重新发布；专用消费者 `knowledge-object-cleanup-v1` 写入回执，原文件和确定性解析产物两个 MinIO 对象均返回不存在 |

双视口布局与控制台结果如下：

| 视口 | 页面横向边界 | 表格局部滚动 | 控制台与视觉检查 |
| --- | --- | --- | --- |
| `1440×900` | `body/root scrollWidth = 1440`，无页面级横向溢出 | 容器 `818 px`、内容 `1120 px`，局部横向滚动成立 | 知识组织导航、筛选、空状态和操作区无重叠；本地应用来源错误为 `0` |
| `390×844` | `body/root scrollWidth = 390`，无页面级横向溢出 | 容器 `328 px`、内容 `1120 px`，最大滚动距离 `792 px` 且实测到达末端，“更新时间/操作”列可见 | 顶栏、主操作、导航、筛选和空状态无重叠；本地应用来源错误为 `0` |

Chrome 扩展自身的令牌失效和请求超时日志来自 `chrome-extension://`，不属于当前 Web 应用；验收按 `http://127.0.0.1:3000` 来源筛选后无错误。完成检查后已恢复浏览器原视口。

## 5. `P6A-03` 验证记录

### 5.1 已实现范围

- 文档详情读模型聚合完整文档元数据、当前发布指针、文件夹、标签、收藏和不可变版本历史；每个版本展示来源文件、类型、大小、安全扫描、解析器、OCR、页数、内容块、最新索引状态、构建批次和 Chunk 数量，处理失败时展示稳定错误码和可公开错误详情。
- 详情响应在协议边缘明确排除 `original_object_key`、解析产物键和内容正文；领域内部对象键只服务于受控对象读取。OpenAPI、Python 契约和前端生成类型已重新生成并通过漂移与兼容检查。
- 新增 `knowledge.document.download` 权限，资源类型固定为 `document`，PDP 使用路径中的文档 ID 执行资源级授权。Revision `20260825_0073` 同步 Registry 26 的详情/下载绑定和菜单发布；权限回填只作用于存量系统所有者角色，自定义角色继续由管理员明确授权。
- 下载由同源 API 代理返回原文件字节，浏览器不接触 MinIO 地址或对象键。服务在对象读取前后分别打开短事务，重新校验活动成员、空间、知识库、文档、版本、来源和授权描述；任一关系变化、对象缺失或大小不一致均失败关闭。
- 下载成功审计只保存文档、版本、来源和文件大小业务事实，不保存对象键；跨空间详情和下载统一表现为不可见。
- 知识页面新增文档详情抽屉、版本下载、列表/卡片视图切换；卡片视图保留详情、选择、上传新版本、确认就绪、发布、移动、标签、收藏和删除等既有操作。上传、发布、重试和批量操作继续复用原事实服务，不新增在线正文、AI 写回或协作模型。

### 5.2 自动化与真实依赖

已执行以下定向验证：

```bash
pnpm --filter @ai-platform/web test --run
AI_PLATFORM_TEST_DATABASE_URL='postgresql+psycopg://ai_platform:local-development-only@127.0.0.1:5432/ai_platform' \
  MYPYPATH=apps/api/src:apps/worker/src:packages/backend/src:packages/contracts/src \
  .venv/bin/pytest -q tests/integration/test_p1d01_knowledge_postgres.py
.venv/bin/ruff check apps/api/src/ai_platform_api/modules/knowledge \
  tests/integration/test_p1d01_knowledge_postgres.py
./scripts/verify
```

当前结果：前端 `23` 个测试文件、`73/73` 个测试通过；真实 PostgreSQL 知识集成 `7/7` 通过，覆盖详情聚合、下载字节、跨空间不可见、成功审计和 API 映射不泄露对象键。Revision `20260825_0073` 的非空升级、安全降级和再升级专项 `2/2` 通过，确认 Registry 26 文档详情/下载绑定、既有 Owner 授权和不可变菜单快照同步迁移，并在自定义授权或后续菜单发布存在时拒绝破坏性降级。此前扩大回归中的 PostgreSQL、入库任务和 MinIO 定向集成 `13/13`、资源注册表 `6/6` 均通过。最终原样 `./scripts/verify` 的 Web `73/73`、Python `974/974`、Ruff、mypy strict（`775` 个源文件）、注释、契约、权限注册表、生产构建和供应链检查全部通过。

### 5.3 浏览器与真实数据流

2026-08-25 使用全合成本地账号、个人空间、知识库和仓库固定合成文件执行真实页面与服务数据流，不使用 Mock API：

| 场景 | 实际结果 |
| --- | --- |
| 页面上传 | 在内置浏览器选择 `synthetic-text.pdf` 并提交，页面提示“文档已上传并进入解析队列”，列表状态从“待解析”真实流转到“待发布” |
| 首版发布与新版本 | 首个合成文档 V1 通过页面确认发布后成为当前发布版；V2 上传后完成解析和索引但保持待发布，详情同时展示 V2、V1 与唯一当前发布指针 |
| 处理链详情 | Markdown V1 展示 `markdown-structural-v1`、未使用 OCR、`1/4` 页数/内容块、构建批次 `#1` 和 `1` 个 Chunk；Text V2 展示 `plain-text-v1`、`1/2` 页数/内容块及独立索引状态 |
| 授权下载 | 页面点击下载后数据库新增成功审计；审计属性不含 `workspaces/...` 对象键。正式下载接口返回 SHA-256 `2f2537f0…8933870a`，与合成原件完全一致 |
| 卡片视图 | 列表切换为卡片后仍可打开同一详情、选择文档、发布及打开完整操作菜单；前端组件测试同时覆盖收藏回调和下载版本标识 |
| 协议泄露 | 详情 API 映射回归明确断言响应既不含 `original_object_key` 字段，也不含对象键值；浏览器详情只显示来源和安全元数据 |

双视口实际结果：

| 视口 | 页面与抽屉边界 | 视觉与控制台 |
| --- | --- | --- |
| `1440×900` | `body/root scrollWidth = 1440`，主内容 `1200 px`；详情抽屉、双版本处理链和卡片无页面级横向溢出 | 信息层级、按钮和版本分隔无重叠；修复 Drawer 弃用属性后，本地来源 warning/error 为 `0` |
| `390×844` | `body/root scrollWidth = 390`，抽屉宽度及右边界均为 `390 px`；卡片左右边界为 `31–359 px` | 顶栏、上传入口、组织导航、筛选、卡片、详情和下载按钮无重叠；本地来源 warning/error 为 `0` |

Chrome 扩展未开放本地文件 URL 权限时无法由自动化注入文件；这不作为产品上传失败。相同页面上传已改由内置浏览器真实完成，Chrome 继续用于 Blob 下载、桌面/移动布局和控制台验收。完成检查后已恢复浏览器默认视口。

## 6. `P6A-04` 验证记录

### 6.1 已实现范围

- 新增 `GET /personal-workbench` 聚合个人知识库数、活动文档数、已发布数、收藏数、活动索引覆盖数和待索引数；最近文档按当前账号服务端访问时间和更新时间排序，收藏文档使用同一低敏投影。
- 新增 `POST /personal-workbench/accesses`，只在文档详情成功打开后记录最近访问；`workspace_id + account_id + document_id` 唯一键和 PostgreSQL Upsert 保证重复访问只更新时间，同一事务写入审计事实。
- 新增 `GET /knowledge-search`，支持名称/正文/全部匹配、知识库、仅收藏、分页和稳定排序。正文只搜索当前发布版本对应的活动 `IndexVersion` 与活动 Chunk，不读取草稿、历史发布、已撤销索引或已删除文档。
- 搜索先冻结可信 `RequestContext`、资源范围、密级和字段遮罩；标题不可见时不读取标题列，正文不可见时生成的 SQL 不引用 `retrieval_chunks`，避免“响应已遮罩但查询仍读取”的边界漏洞。跨空间资源继续表现为不可见。
- Revision `20260825_0074` 新增 `document_accesses`，生命周期 Registry 升至 18，权限 Registry 升至 27，并为工作台读取、最近访问写入和全局搜索三个操作补齐接口绑定与存量菜单发布升级；旧 `P6A-03` 专项显式停留在 `0073`。
- 个人空间总览替换为紧凑知识工作台，企业空间继续使用原企业总览；个人空间不再请求企业权益接口。页面包含统计、统一搜索、匹配范围、知识库/收藏筛选、分页、索引降级提示、最近文档、收藏、最近问答和空/错/加载状态。
- 快捷上传和新建文件夹复用既有知识页面动作；成功打开文档后先记录访问再跳转详情。快捷问答仅通过 URL 预填 Prompt，保留当前会话深链，不提前实现 `P6A-05` 的知识库/标签范围。
- 第一版排除项没有变化：未新增在线文档编辑器、文档 AI 写回、多人实时协作或第二套正文模型。

### 6.2 自动化与门禁

已执行以下验证：

```bash
.venv/bin/pytest
pnpm --filter @ai-platform/web test --run
./scripts/verify
git diff --check
```

2026-08-30 的原样 `./scripts/verify` 全部通过。Python 测试为 `987/987`；其中 `P6A-04` 单元测试 `10/10`、PostgreSQL/HTTP/Migration 专项 `3/3`。Web 为 `24` 个测试文件、`81/81`，包含工作台、搜索、访问记录和 Assistant URL 预填回归。Ruff、mypy strict（`778` 个源文件）、Prettier、ESLint、TypeScript、OpenAPI 与生成类型漂移、Registry、架构、注释、UnoCSS、供应链和生产构建均通过。

### 6.3 容器与浏览器验收

使用 `AI_PLATFORM_MIN_FREE_DISK_GB=45 ./platform start` 对本次进程降低启动预检门槛并完成最终镜像重建，仓库默认 `50 GB` 安全值未修改。数据库已升级到 Revision `20260825_0074`，`./platform doctor` 的 API、配置、PostgreSQL、Valkey、MinIO、Tika、Web 和 Worker 等 13 项诊断全部通过。

浏览器验收使用明确标记的全合成账号、知识库、文档和 Markdown 夹具，真实完成上传、解析、索引、发布、标题搜索、正文搜索、收藏、仅收藏筛选、访问记录、详情跳转和问答 Prompt 预填。桌面 `1440×900` 的 `body/root scrollWidth` 均为 `1440`；移动端 `390×844` 的 `body/root scrollWidth` 均为 `390`，越界元素为 `0`。两种视口均无页面级横向溢出、控件重叠或文本遮挡，本地来源控制台 warning/error 为 `0`。验收后已恢复默认视口并保留个人工作台页面供人工查看。

## 7. P6A-05 验证记录

### 7.1 已实现范围

- 会话支持 `workspace` 与 `selected` 两种知识范围；指定范围由知识库和标签组成，多个标签采用并集后与知识库范围求交集，空标签范围保持为空，不回退到全空间。
- 创建 Assistant Run 时冻结知识库、文档和附件 ID；活动 Run 期间禁止修改范围和删除附件，确保检索计划与模型上下文使用同一份授权快照。
- 临时附件只允许 UTF-8 文本，最多 3 个，单个最多 20,000 字节；附件正文仅进入当前 Run 的不可信上下文，不写入知识库、索引或文档正文，归档会话时清理附件正文。
- 提供“总结要点”“提取行动项”“对比差异”“依据来源回答”四项快捷指令；支持活动/已归档筛选、归档确认、已归档会话只读和历史消息来源查看。
- 所有 HTTP、SSE、审计、权限与生命周期绑定继续通过可信 `RequestContext`、`workspace_id` 资源级授权、字段权限和跨空间隔离边界。真实供应商质量、成本和数据政策仍受阶段 5 门禁约束，本地 Mock 结果不替代真实开通结论。

### 7.2 自动化、运行和浏览器验收

专项 Python `4/4`、Web `8/8` 通过；数据库已升级到 Revision `20260830_0075`，`./platform doctor` 13 项全部通过。真实页面使用明确标记的合成账号、知识库、文档和文本夹具，完成知识库创建、文档上传、指定范围保存、临时附件添加/删除、快捷指令预填、问答引用、Run 冻结、归档及已归档只读验证。

桌面 `1440×900` 与移动 `390×844` 的 `body/root scrollWidth` 均等于视口宽度，页面级横向溢出为 `0`；本地应用控制台 warning/error 均为 `0`。归档后活动列表为空，切换“已归档”可查看历史消息并显示“该会话已归档，只能查看历史”提示。

## 8. P6A-06 阶段 A 联合验收记录

### 8.1 冻结清单与覆盖范围

P6A-06 使用 `tests/fixtures/e2e/p6a06-v1.json` 冻结清单及对应的两个 JSON Schema。清单声明仅使用合成数据、失败关闭、前置和恢复诊断必须执行，并固定证据路径 `.ai-platform/evidence/p6a06-latest.json`。12 个场景覆盖目录/标签/收藏/回收站生命周期、版本发布事务、解析租约与有界重试、索引发布/重建/撤销原子性、检索权限与不可变快照、工作台统计/搜索/最近访问、助手范围与附件冻结/归档、标签收窄和空范围失败关闭、详情下载 Migration、工作台 Migration、助手 Migration 以及个人空间和企业部门的跨空间拒绝。

### 8.2 自动化验证

执行入口为 `./platform accept-stage-6-a`，内部依次运行前置 `./platform doctor`、冻结清单中的 12 个 PostgreSQL pytest 节点和恢复 `./platform doctor`；任一前置或场景失败都会失败关闭并生成低敏证据。2026-08-31 实际结果如下：

| 检查项 | 结果 |
| --- | --- |
| 前置 `./platform doctor` | 13/13 诊断通过 |
| 冻结场景 | 12/12 通过，观察用例数与期望一致 |
| 恢复 `./platform doctor` | 13/13 诊断通过 |
| 验收器单元测试 | `tests/test_p6a06_acceptance.py` 7/7 通过 |
| 证据校验 | `stage-6-a-acceptance-evidence.v1.schema.json` 通过 |
| 数据库 Revision | `20260830_0075` |

证据文件只保存状态、计数、耗时、清单摘要和仓库状态，不保存业务正文、对象键、账号、Cookie 或 Token。验收运行发生在本节点提交前，因此证据中的 `repository_dirty=true` 是准确事实；提交后如需重跑，应以新的干净 Revision 证据替换，不能沿用旧运行描述。

### 8.3 浏览器证据边界与阶段结论

P6A-06 的浏览器证据复用 P6A-02～P6A-05 已完成的真实合成数据双视口验收：桌面 `1440×900` 与移动 `390×844` 均无页面级横向溢出，核心页面控制台 warning/error 为 `0`，并已覆盖组织、文档详情/下载、工作台搜索和助手范围/归档流程。P6A-06 联合验收器本身只执行平台诊断和 PostgreSQL pytest，不自动驱动浏览器，也不把阶段 5 的真实供应商质量、成本、数据政策或容量状态标记为通过。

阶段 A 的六个节点均已完成，下一阶段节点为 `P6B-01` 企业控制台聚合页。

## 9. P6B-01 企业控制台聚合页验证记录

### 9.1 已实现范围与安全边界

- 新增 `GET /api/v1/workspaces/{workspace_id}/enterprise-console`，按统一时间窗返回企业空间摘要、成员/知识库/活动文档/已发布/处理中/失败统计、存储用量与额度、近 1～12 个月趋势和最近 1～50 条内容；响应声明生成时间和最终一致性，不把异步聚合冒充强一致事实。
- 处理数量同时覆盖入库和索引队列，并按文档 ID 去重；月度趋势使用 PostgreSQL 单一 `date_trunc` 表达式复用，避免 SQLAlchemy 为 `GROUP BY` 生成不一致绑定参数。
- 应用服务要求可信浏览器 Session、路径与 Header 当前企业空间一致、活动成员、`workspace.overview.access` 和工作空间级完整授权；默认 PDP 拒绝、个人空间、跨空间以及部门/自身/资源级窄范围全部失败关闭。
- 文档统计、趋势和最近内容按 `authorized_maximum_security_level` 裁剪；最近内容还受标题与知识库名称字段遮罩约束，字段不可见时不发起对应查询。响应只返回低敏元数据，不返回正文、对象键、解析产物键、账号、Cookie 或 Token。
- 企业描述和 Logo 当前没有持久化字段，接口明确返回 `null`，页面说明由后续企业资料节点开放维护；本节点没有提前创建空资料模型或虚假保存入口。
- 企业总览组合新控制台与既有空间概况、资源配额和 Open API；个人空间继续显示个人工作台。企业页面提供成员管理、组织架构、知识生产和知识助手四个真实路由入口，并覆盖加载、错误、空数据和响应式状态。
- Registry 升至 29，新增企业控制台 API 与空间总览动作绑定；Revision `20260831_0076` 为仍处于确定性当前发布的存量空间生成不可变 Registry 29 菜单快照，存在后续发布时拒绝破坏性降级。

### 9.2 自动化、Migration 与容器验证

已执行 P6B-01 应用、HTTP、PostgreSQL 聚合、Migration、历史 Head 断言和完整门禁。实际结果如下：

| 检查项 | 结果 |
| --- | --- |
| P6B-01 应用、HTTP 和 PostgreSQL 专项 | 15/15 通过 |
| 真实 PostgreSQL 聚合与 Revision `0076` 专项 | 3/3 通过 |
| 扩大 Migration 回归 | 32/32 通过 |
| 历史 Head 断言专项 | 12/12 通过 |
| 原样 `./scripts/verify` | Web 26 个测试文件、89/89；Python 1011/1011；Ruff、mypy strict（785 个源文件）、Prettier、ESLint、TypeScript、OpenAPI 漂移、Registry、契约兼容、模块依赖、注释、UnoCSS、供应链和生产构建全部通过 |
| 容器恢复 | 首次 Docker Hub 元数据连接超时；随后以 `COMPOSE_PARALLEL_LIMIT=1 AI_PLATFORM_MIN_FREE_DISK_GB=45 ./platform start` 串行重建成功，仓库默认 50 GB 安全值未修改 |
| 恢复 `./platform doctor` | 13/13 诊断通过，数据库 Revision 为 `20260831_0076` |

### 9.3 浏览器与真实合成数据验收

使用正式本地 API 创建明确标记的合成企业空间、所有者、成员、知识库和三篇文档，不使用真实邮箱、密码或企业资料，也不使用 Mock API 替代页面数据流。聚合接口与页面实际显示 2 名活动成员、1 个活动知识库、3 篇活动文档、0 篇已发布、0 篇处理中、0 篇失败、0/100 GB 存储，以及最近三篇合成文档；六个月趋势为 `0、0、0、0、0、3`。

| 视口 | 页面边界与交互 | 控制台与入口 |
| --- | --- | --- |
| `1440×900` | `body/root scrollWidth = 1440`，无页面级横向滚动或控件重叠；横幅装饰圆被容器裁剪且不影响交互 | 本地 warning/error 为 0；知识助手入口真实跳转到 `/workspace/assistant` 后可返回控制台 |
| `390×844` | `body/root scrollWidth = 390`，企业统计、趋势、存储、最近内容和四个治理入口均可见，无页面级横向滚动或控件重叠 | 本地 warning/error 为 0；唯一几何越界仍是容器内裁剪的非交互装饰圆 |

验收结束后已恢复默认视口并保留企业控制台页面供人工查看。P6B-01 已通过交付门禁，下一独立节点为 `P6B-02` 团队管理产品页。

## 10. P6B-02 团队管理产品页验证记录

### 10.1 已实现范围与安全边界

- 新增 `GET /api/v1/workspaces/{workspace_id}/team-management`，在同一事务口径聚合团队摘要、成员、邀请、部门、岗位、活动自定义角色、成员有效角色来源、最后活跃和最近治理审计；姓名与登录名分别执行字段遮罩，响应不返回密码、Cookie、Token 或内部授权缓存。
- 新增邀请撤销、成员原子配置、成员恢复和成员移除接口，并复用既有邀请创建与接受接口。页面交付成员搜索、状态/部门/岗位/有效角色筛选与 URL 恢复、一体化编辑抽屉、邀请链接复制、完整邀请状态、成员生命周期操作和低敏治理时间线。
- 所有接口要求可信浏览器 Session、路径与 Header 当前企业空间一致、活动 Owner、当前权限和目标资源范围；其他邀请或成员的允许决策不能复用于当前目标。成员组织、岗位和自定义直接角色使用乐观版本一次替换，成员事实、授权版本、审计与 Outbox 在同一事务提交。
- Owner 永远不可编辑、停用或移除。停用保留部门、岗位和自定义直接角色，但有效角色立即为空；恢复只允许 `disabled → active` 并让保留配置重新生效。移除进入 `left` 终态，清除组织、岗位和自定义直接角色，重新加入必须创建新邀请。
- 邀请仅允许 Owner 撤销仍有效的 `pending` 记录，过期状态按 `expires_at` 计算；已接受、已撤销、已过期和跨空间邀请均拒绝再次撤销。成员编辑只维护部门、主部门、岗位和自定义直接角色，不修改系统角色、空间角色或部门继承角色。
- 角色事务提交后，PDP 可能因 PostgreSQL `role_version` 见证传播窗口短暂返回可重试 `POLICY_UNAVAILABLE`。前端查询只对该稳定错误码执行 500/1000/2000/4000 ms 有限退避；`POLICY_DENIED`、业务冲突、其他依赖错误和未知错误均立即失败，不以重试扩大权限。
- Registry 升至 30，新增 5 项团队治理权限、5 个 API 资源和 5 个菜单动作。Revision `20260831_0077` 只为存量企业空间的系统 Owner 回填权限并追加不可变菜单快照，不给个人空间或自定义角色隐式授权；存在自定义授权或后续菜单发布时拒绝破坏性降级。

### 10.2 自动化、Migration 与容器验证

最终验证结果如下：

| 检查项 | 结果 |
| --- | --- |
| P6B-02 单元、HTTP 与真实 PostgreSQL 专项 | 团队聚合、资源级授权、字段遮罩、跨空间拒绝、乐观冲突、邀请撤销、配置原子性、停用/恢复/移除、审计与 Outbox 场景全部通过 |
| 前端团队页面与 Hook | 7/7 通过，覆盖页面状态、URL 筛选、Owner/`left` 边界、邀请、成员配置和仅限可重试策略不可用的有限退避 |
| 原样 `./scripts/verify` | Web 28 个测试文件、96/96；Python 1031/1031；Ruff、mypy strict（794 个源文件）、Prettier、ESLint、TypeScript、OpenAPI 漂移、Registry、契约兼容、模块依赖、注释、UnoCSS、Secret Scanner、供应链和生产构建全部通过 |
| 容器恢复 | 以 `COMPOSE_PARALLEL_LIMIT=1 AI_PLATFORM_MIN_FREE_DISK_GB=45 ./platform start` 串行重建成功，仓库默认 50 GB 安全值未修改 |
| `./platform doctor` | Web、API、MinIO、Tika、PostgreSQL、Valkey、六类 Worker/Scheduler 共 13 项全部通过，数据库 Revision 为 `20260831_0077` |

### 10.3 真实合成数据与浏览器验收

使用正式本地 API 创建明确标记的全合成 Owner、普通成员、待移除成员、企业空间、部门、岗位、自定义角色和邀请，不使用用户真实邮箱、密码或企业资料，也不直接绕过业务表写入。真实数据流完成成员邀请创建与接受、组织/岗位/直接角色原子配置、停用、恢复、移除和邀请撤销，并读取团队聚合核对终态：

- 活动成员配置提交后版本递增，页面同时显示部门、岗位、直接角色和空间角色；停用后部门、岗位和直接角色保留而有效角色为空，恢复后两类角色重新生效。
- 待移除成员先配置组织和直接角色；移除后状态为 `left`，部门、主部门、岗位、直接角色和有效角色全部清空，页面只显示“需通过新邀请重新加入”，不存在恢复入口。
- 待处理邀请撤销后显示“已撤销”，不存在再次撤销入口；摘要最终显示启用成员 2、待接受邀请 0、已停用 0、部门 1 和岗位 1。
- 治理时间线包含配置更新、停用、恢复、移除和邀请撤销，结果均为成功。姓名/登录名搜索以及状态、部门、岗位、有效角色筛选均写入 URL 并只返回正确成员，重置后 URL 与全量列表恢复。

双视口结果如下：

| 视口 | 页面边界与交互 | 控制台 |
| --- | --- | --- |
| `1440×900` | `body/root scrollWidth = 1440`，摘要、筛选、成员/邀请表和时间线无页面级横向溢出或控件重叠；Owner、活动成员和终态成员操作入口正确 | 本地 warning/error 为 0 |
| `390×844` | `body/root scrollWidth = 390`；筛选换行正常，成员表和邀请表分别在 352 px 局部容器中横向滚动，末端“移除”和“复制链接”操作完整可达；无页面级横向溢出 | 本地 warning/error 为 0 |

验收结束后已恢复 `1280×720` 默认视口并保留团队管理页供人工查看。P6B-02 已通过交付门禁，下一独立节点为 `P6B-03` 企业分类与团队知识域。

## 11. P6B-03 企业分类与团队知识域验证记录

### 11.1 已实现范围与安全边界

- 企业分类支持创建、读取、编辑、父子层级、文档整体绑定替换、公开/指定部门/私有策略和归档；存在活动子分类时拒绝归档父分类，并返回稳定错误码 `CATEGORY_HAS_ACTIVE_CHILDREN`。
- 团队知识域支持创建、编辑、成员/部门/知识库声明范围原子替换、不可变 RAG 策略版本、有效范围解释和归档；直接成员关系使用企业 `membership_id`，不使用账号 ID 代替。
- 分类和知识域只保存治理关系，不复制文档、版本、来源、对象、解析产物、Chunk 或索引。RAG 策略更新新增不可变版本，历史策略继续可追溯。
- 有效范围固定为声明知识库与当前 PDP 授权的交集；空范围、PDP 失败、成员失效和知识域归档均失败关闭，不回退到全企业。
- 所有读写操作要求可信浏览器 Session、路径与 Header 当前企业空间一致、活动成员和目标资源级授权；写操作额外执行乐观版本，并在同一事务内提交领域事实、审计和 Outbox。
- Registry 升至 31，Revision `20260831_0078` 新增企业分类、文档绑定、知识域范围和策略版本事实，并纳入工作空间生命周期注册表。

### 11.2 自动化、Migration 与容器验证

最终验证结果如下：

| 检查项 | 结果 |
| --- | --- |
| P6B-03 后端生命周期、HTTP 与错误契约 | 32/32 通过，覆盖层级、策略、绑定、范围交集、乐观冲突、跨空间拒绝、归档、审计与 Outbox |
| P6B-03 前端页面、Hook 与 API Service | 9/9 通过，覆盖创建、编辑、成员标识、范围保存、错误提示和归档状态 |
| Migration 往返 | `base → head → base → head` 8/8 通过，Revision 为 `20260831_0078` |
| 原样 `./scripts/verify` | Web 31 个测试文件、105/105；Python 1038/1038；Ruff、mypy strict（809 个源文件）、Prettier、ESLint、TypeScript、OpenAPI 漂移、Registry、契约兼容、模块依赖、注释、UnoCSS、Secret Scanner、供应链和生产构建全部通过 |
| `./platform doctor` | Web、API、MinIO、Tika、PostgreSQL、Valkey、六类 Worker/Scheduler 共 13 项全部通过 |

### 11.3 真实合成数据与浏览器验收

使用正式本地 API 和页面创建明确标记的合成企业、部门、知识库和文档，不使用真实邮箱、密码或企业资料，也不在证据中保存 Cookie、Token、对象键或业务正文。真实数据流完成根/子分类创建和层级核对、名称/说明编辑、文档绑定清空与重新绑定、公开/指定部门/私有策略切换、知识域创建编辑、成员/部门/知识库范围原子保存、有效范围解释、RAG 模式更新以及经用户授权后的分类和知识域归档。

验收期间发现并修复两个产品流程缺口：直接成员下拉原本提交 `account_id`，现已改为服务端关系所需的 `membership_id` 并增加回归测试；父分类存在活动子分类时原本只能显示通用乐观冲突，现已增加稳定错误码和“分类仍存在活动子分类，请先归档子分类”的明确提示。随后依次归档子分类、根分类和知识域，页面活动分类、团队知识域、已分类文档和受治理知识库统计均归零，归档历史、父子关系和三版 RAG 策略仍可追溯；数据库审计和 Outbox 均存在创建、更新、绑定、范围替换和归档事实。

双视口结果如下：

| 视口 | 页面边界与交互 | 控制台 |
| --- | --- | --- |
| `1440×900` | `body/root scrollWidth = 1440`，主内容宽 1200 px，创建、编辑、范围、解释和归档入口均在页面边界内 | 本地 warning/error 为 0 |
| `390×844` | `body/root scrollWidth = 390`，无页面级横向滚动；主导航和全部治理入口可达，归档态无文字遮挡。一个装饰性容器局部右边界为 428 px，但未扩大页面宽度 | 本地 warning/error 为 0 |

Docker Hub 基础镜像元数据在重建时连续超时，因此本轮使用已有本地镜像做增量覆盖以完成 API/Web 运行验收；仓库默认 Docker 磁盘安全值未修改，临时覆盖文件未进入工作树或提交。验收结束后已恢复默认视口并保留本地企业知识库标签供后续节点使用。P6B-03 已通过交付门禁，下一独立节点为 `P6B-04` 企业文档发布审批门户。

## 12. P6B-04 企业文档发布审批门户验证记录

### 12.1 已实现范围与安全边界

- 企业分类新增 `approval_required`；发布申请只接受服务端授权后状态为 `ready` 且索引为 `ready` 或 `active` 的不可变文档版本，冻结内容哈希、版本、分类和治理摘要。
- 发布请求复用通用审批引擎，支持多级待办、通过、驳回、撤回、超时和转交；同一版本已有待审批申请时，服务端幂等约束和前端候选过滤共同阻止重复申请。
- 最终批准前重新鉴权并复核申请人成员状态、发布权限、文档、版本、索引和治理事实；只有批准且复核成功时才原子切换文档与索引发布指针。失败进入 `publish_failed`，旧指针保持不变。
- 审批、发布请求、审计和 Outbox 在同一事务提交；可信 `RequestContext`、路径/Header 企业空间一致、`workspace_id`、目标资源级授权和跨空间隔离均为服务端硬边界。
- Registry 升至 32，生命周期 Registry 升至 21，数据库 Revision 为 `20260831_0079`。第一版仍不新增在线正文、文档 AI 写回或多人实时协作。

### 12.2 自动化、Migration 与容器验证

| 检查项 | 结果 |
| --- | --- |
| P6B-04 后端单元、真实 PostgreSQL 与 API 专项 | 15/15 通过，覆盖申请、重复幂等、多级审批、撤回、驳回、超时、转交、最终重新鉴权、治理漂移、发布失败、指针原子性、跨空间、审计与 Outbox |
| P6B-04 前端页面、Hook 与 API Service | 14/14 通过，覆盖台账、详情、候选加载、重复过滤、申请、通过、驳回、转交、撤回和页面状态 |
| 原样 `./scripts/verify` | Web 31 个测试文件、110/110；Python 1051/1051；Ruff、mypy strict（814 个源文件）、Prettier、ESLint、TypeScript、OpenAPI/生成类型漂移、Registry、生命周期 Registry、模块依赖、注释结构、Secret Scanner、SBOM、供应链和 Vite 生产构建全部通过 |
| `./platform doctor` | Web、API、MinIO、Tika、PostgreSQL、Valkey、六类 Worker/Scheduler 共 13 项全部通过，数据库 Revision 为 `20260831_0079` |

### 12.3 正式合成数据、Worker 与浏览器验收

通过正式 HTTP 业务接口创建明确标记的合成 Owner、两级审批人、企业空间、成员关系、最小审批权限、两级策略、知识库、审批分类和文档；成员均通过正式邀请与接受流程加入，没有直接写业务表，也未使用真实邮箱、密码、Cookie 或企业资料。上传合成文本后由真实 Worker 完成解析和索引。

真实页面依次完成 V1 申请、同版本待审批候选过滤、Owner 撤回、重新申请、一级通过和二级最终通过；正式文档详情确认当前发布指针指向 V1，V1 版本为 `published`，对应索引为 `active`，发布时间与索引激活时间一致。随后上传 V2 并由真实 Worker 推进至版本和索引均 `ready`，再完成申请、一级审批人转交和二级审批人填写稳定原因码后驳回；终态确认发布指针仍指向 V1，V1 继续保持 `published/active`，V2 保持 `ready/ready` 且未激活。

双视口结果如下：

| 视口 | 页面边界与交互 | 控制台 |
| --- | --- | --- |
| `1440×900` | `body/root scrollWidth = 1440`，审批区域、申请入口和驳回终态均可见，无页面级横向溢出 | warning/error 为 0 |
| `390×844` | `body/root scrollWidth = 390`，审批区域和驳回终态均可见，无页面级横向溢出 | warning/error 为 0 |

超时、申请人停用、审批期间撤权、治理漂移和发布失败由后端专项自动化覆盖，本次浏览器验收不声称逐一实操这些故障场景。普通审批人页面仍可能显示其系统 Member 角色本身获权的部分治理入口；本轮仅确认审批按钮与参与者可见性按照服务端责任生效，不声称所有非审批治理按钮均已隐藏。

Docker Hub 基础镜像元数据请求因外部网络 `DeadlineExceeded` 无法完成全栈和 Web 单镜像重建；本轮将完整门禁生成的 `apps/web/dist` 临时部署到健康 Web 容器并 reload Nginx 完成运行验收，之后 `./platform doctor` 仍为 13/13。仓库默认 50 GB Docker 磁盘安全值未修改，临时 35 GB 仅用于预检；临时静态产物、合成运行数据和任何凭证均未进入提交。

## 13. `P6B-05` 权限管理与审计页验证记录

### 13.1 已实现范围与安全边界

- 角色治理聚合接口一次返回系统/自定义角色、可信 `role_version`、授权矩阵、影响成员、可编辑状态、权限目录、数据范围、安全等级和字段遮罩；系统角色、停用角色和无管理权限角色只读。
- 权限整体替换使用可信角色版本执行乐观锁；陈旧页面返回稳定 `ROLE_CONFLICT`，前端失效旧快照并提示重新确认。授权、版本、审计和 Outbox 在同一事务提交，不允许部分成功。
- 公共 V1 契约保留兼容窗口：旧客户端可省略 `expected_role_version`，Router 先读取服务端可信版本；P6B-05 新页面始终显式提交版本。审计主体被字段遮罩时使用零 UUID 和 `actor_id_masked=true` 维持旧字段类型，页面明确显示“受字段权限保护”。
- 审计列表不返回自由属性；详情按 `workspace_id + audit_id` 隔离并使用白名单投影，对敏感键和嵌套结构递归脱敏。跨空间请求由可信 `RequestContext` 和资源级策略失败关闭。
- 异步导出在创建时冻结筛选、字段遮罩和时间上界，使用客户端幂等键；审计与 Outbox 同事务登记。Worker 使用租约、有限重试、死信和幂等处理生成确定性 JSONL SHA-256 安全摘要，页面不暴露对象键、内部哈希输入或原始导出文件。
- 独立页面 `/workspace/permissions-audit` 提供角色选择、影响成员、可编辑权限矩阵、并发冲突恢复、审计 URL 筛选、详情 Drawer 和异步导出轮询；个人空间明确不启用企业权限治理。

### 13.2 自动化、Migration 与运行诊断

数据库 Revision 为 `20260903_0080`，权限 Registry 为 35、字段策略 Registry 为 3、生命周期 Registry 为 22。完整 `./scripts/verify` 原样通过：Web 33 个测试文件 114/114，Python 1064/1064，mypy strict 检查 825 个源文件；Ruff、Prettier、ESLint、TypeScript、生产构建、OpenAPI/生成类型漂移、V1 契约兼容、Registry、Migration、架构、注释、UnoCSS、SBOM、许可证和供应链检查全部通过。P6B-05 两组真实 PostgreSQL 集成测试 9/9，Migration 往返与 Registry 专项 14/14；历史 Migration/Registry 镜像更新后的回归集合 23/23 通过。

本地启动使用进程级 `AI_PLATFORM_MIN_FREE_DISK_GB=35`，主机实际约 40 GB 可用磁盘；仓库默认 50 GB 安全值未修改。镜像构建、Migration 和全部容器启动成功，`./platform doctor` 的 Web、API、MinIO、Tika、PostgreSQL、Revision、Valkey、五类 Worker 和 Scheduler 共 13/13 通过。

### 13.3 真实合成数据与双视口验收

使用明确标记的全合成账号、企业空间、自定义角色、角色绑定和审计记录完成正式 HTTP 登录与可信空间切换。个人空间显示“个人空间不启用企业权限治理”；企业空间可进入权限页，系统 Owner 角色只读，自定义角色可编辑且显示影响成员。另一可信会话先把角色版本从 2 更新为 3，浏览器再以陈旧版本保存，真实触发 `ROLE_CONFLICT` 和“已载入最新版本”提示；重新确认后成功保存，最终角色版本为 5，权限事实同时包含 `knowledge.base.read` 和带 `attributes` 字段遮罩的 `operations.records.read`。

审计页真实验证 `action` 筛选写入 URL 并在刷新后恢复；点击记录打开详情 Drawer，扩展属性仅显示服务端白名单化的 `{}`。按冻结筛选创建异步导出后，真实 Worker 在一次尝试内完成 2 条记录，结果 SHA-256 为 `35f39c369c6df025f1cb67d151af43e8fe9b0d0553478456ca7ae1ad91d4c390`，页面只显示“已生成 2 条脱敏审计记录的安全摘要”。以个人空间上下文访问企业审计详情返回 `403 POLICY_DENIED`，未返回目标资源事实。

双视口结果如下：

| 视口 | 页面边界与交互 | 控制台 |
| --- | --- | --- |
| `1440×900` | `body/root scrollWidth = 1440`，审计筛选为四列，无页面级越界元素 | warning/error 为 0 |
| `390×844` | `body/root scrollWidth = 390`，筛选切换为单列；角色矩阵容器 `clientWidth=312`、`scrollWidth=836`，审计表宽 900，仅在各自局部容器横向滚动 | warning/error 为 0 |

## 14. 当前限制与下一步

- `P6B-01`～`P6B-05` 的代码、契约、真实 PostgreSQL 测试、统一门禁、容器运行和双视口浏览器验收均已通过；P6B-05 当前只等待独立 Git 提交，之后继续 `P6B-06`。
- 阶段 A 与 `P6B-01`～`P6B-05` 已完成实现和验证；这些本地机制和合成数据结论不改变阶段 5 真实供应商质量、成本和数据政策门禁。
- 第一版仍只管理上传文件及其不可变版本；在线正文、文档 AI 写回和多人实时协作继续保留在 `POST-EDITOR-01`、`POST-COLLAB-01`，本节点没有预建入口或空模型。
- 存量数据规模盘点必须在执行目标数据库上以只读摘要完成；当前文档不使用开发库数量推断生产规模。
- 阶段 5 的真实模型质量、价格、固定网络窗口和供应商授权样本仍保持原状态，不由本阶段关闭。

## 15. 阶段结论

`P6A-01` 已形成提交 `f68dc34`，`P6A-02` 已形成提交 `17be077`，`P6A-03` 已形成提交 `186f62f`，`P6A-04` 已形成提交 `92aa2aa`，`P6A-05` 已形成提交 `5d4e39c`，`P6A-06` 已形成提交 `6098ced`，`P6B-01` 已形成提交 `410e9f5`，`P6B-02` 已形成提交 `c32c695`，`P6B-03` 已形成提交 `fae1380`，`P6B-04` 已形成提交 `bddeb28`。P6B-05 已验证待提交；阶段 6 尚未完成，`P6B-06`～`P6B-07` 仍需逐项验收，阶段 B 联合验收、阶段关闭提交和 `stage-6-complete` 标签均未形成。
