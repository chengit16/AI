# AI 智能平台项目进度看板

## 1. 当前状态

| 项目 | 当前值 |
| --- | --- |
| 当前阶段 | 阶段 5：质量运营、合规与规模化 |
| 当前节点 | `P5-08` L3 隔离迁移与恢复（`P5-04` 真实质量门禁受阻，`P5-06` 等待真实价格） |
| 整体状态 | 进行中 |
| 当前架构版本 | V2.8 |
| 容量认证 | `not_run`，不阻塞规划与本地机制建设，不代表生产容量 |
| 真实模型供应商 | `not_configured`，模型网关门禁使用 Mock Provider，索引使用本地确定性 Adapter |
| 最近更新时间 | 2026-08-17 |

## 2. 阶段总览

| 阶段 | 状态 | 完成门禁 | 跟踪文档 |
| --- | --- | --- | --- |
| 阶段 0 | 已完成 | 范围冻结、核心契约和关键技术验证通过 | [阶段 0 技术验证报告](./stages/stage-0-report.md) |
| 阶段 1 | 已完成 | 本地个人版和企业版 MVP 端到端验收通过 | [阶段 1 验证报告](./stages/stage-1-report.md) |
| 阶段 2 | 已完成 | 可靠性、恢复、审计和数据生命周期验收通过 | [阶段 2 验证报告](./stages/stage-2-report.md) |
| 阶段 3 | 已完成 | Agent 发布、灰度和回滚验收通过 | [阶段 3 验证报告](./stages/stage-3-report.md) |
| 阶段 4 | 已完成 | 工具执行权限、幂等、确认和取消验收通过 | [阶段 4 验证报告](./stages/stage-4-report.md) |
| 阶段 5 | 进行中 | 质量、成本、隔离和合规达到目标等级 | [阶段 5 实施计划](./stages/stage-5-plan.md) |
| 条件容量认证 | 待开始 | 获得合适环境后独立验收 | 不阻塞当前正式建设阶段 |
| 项目后置能力 | 待开始 | 按独立价值指标立项 | 不进入当前关键路径 |

## 3. 阶段 0 节点

| 节点 | 交付目标 | 状态 | 验收证据 | Git 提交 |
| --- | --- | --- | --- | --- |
| `P0-01` | 建立节点验收、独立提交和文档跟踪规则 | 已完成 | 2026-08-13：文档存在性、链接路径、节点编号和 `git diff --check` 检查通过 | `0008a25` |
| `P0-02` | 建立 Monorepo、版本约束、目录和本地配置骨架 | 已完成 | 2026-08-13：Node 24、pnpm 11、Python 3.12、锁文件、Ruff、mypy 和敏感文件忽略检查通过 | `f7cecab` |
| `P0-03` | 建立 React Web、FastAPI API 和 Worker 最小健康链路 | 已完成 | 2026-08-13：前后端测试、Lint、类型、构建及 1440/390 像素浏览器验收通过 | `0a52b02` |
| `P0-04` | 固定 OpenAPI、SSE、事件、错误码和领域契约 V1 | 已完成 | 2026-08-13：全部 Schema/Fixture、反例、错误码和 FastAPI OpenAPI 一致性测试通过 | `2cc739b` |
| `P0-05` | 建立 PostgreSQL、Redis、MinIO、Tika 与应用容器编排 | 已完成 | 2026-08-13：七服务健康、首次/二次一键启动、停止保留数据、PostgreSQL 标记复启可读、桌面/移动页面及全量质量门禁通过 | `79cf8c8` |
| `P0-06` | 验证工作空间隔离、策略接口、Trace 和 Outbox 幂等基线 | 已完成 | 2026-08-13：临时 PostgreSQL Schema 从空库 Migration、跨空间越权、字段掩码、策略故障默认拒绝、同事务 Outbox 回滚、Trace 传播和重复消费测试通过；全量 pytest `31/31`，API/Worker 镜像冻结重建通过 | `6a13920` |
| `P0-07` | 验证文档解析、OCR 与切片链路 | 已完成 | 2026-08-13：`p0-07-v1` 7 个全合成固定样本的 SHA-256、格式路由、Tika 3.2.3、Tesseract 5.5、PDF/DOCX/Markdown 表格、OCR、页码、Chunk 稳定 ID 和权限元数据测试通过；全量 pytest `46/46`；中文 OCR 明确后置到阶段 1D 验收 | `d89f8cc` |
| `P0-08` | 验证混合检索、Embedding、Reranker 和引用链路 | 已完成 | 2026-08-13：真实 BGE-M3 与 BGE Reranker 固定快照中文合成证据 Top 1；pgvector 0.8.6、关键词/向量检索、RRF、FastPass、授权精读和引用验证通过；默认镜像不包含模型依赖 | `a02c1ae` |
| `P0-09` | 验证 Mock 模型网关、主备路由、降级和计量 | 已完成 | 2026-08-13：Mock Provider 故障矩阵 `9/9`，主备、有限重试、熔断恢复、私有数据边界、规则降级和 Usage/Trace 记录通过；真实供应商保持未配置 | `b63f96f` |
| `P0-10` | 验证 SSE 事件持久化、断点回放和并发控制 | 已完成 | 2026-08-13：PostgreSQL 随机 Schema 集成测试 `6/6`、领域测试 `7/7`；24 小时保留、5000 事件/10 MB 回放预算、Last-Event-ID、快照恢复、同会话唯一活动 Run、事件幂等和工作空间隔离通过 | `fe67659` |
| `P0-11` | 建立合成评估、越权、字段泄漏和恢复测试数据 | 已完成 | 2026-08-13：`p0-11-v1` 十类全合成安全样本、错误码/范围/字段掩码/恢复守卫和敏感模式校验通过；专项测试 `6/6` | `2fef22d` |
| `P0-12` | 完成许可证/SBOM 检查、阶段报告和范围冻结 | 已完成 | 2026-08-13：Python 首次 7 个已知漏洞经依赖升级清零，Node 生产依赖审计为 0；CycloneDX 1.5、许可证、漂移专项测试 `4/4`；镜像扫描 `not_configured`；阶段 1 计划与范围边界已冻结 | `928c9a6` |
| `P0-13` | 建立前端 UI/UX 布局、导航、响应式和可访问性基线 | 已完成 | 2026-08-13：桌面、移动、窄屏和横屏视口检查通过；阶段 1 优化清单与主色调后置决策已写入 UI/UX 基线文档 | `fdaefea` |
| `P0-14` | 建立适配当前技术栈的 Web 前端代码规范 | 已完成 | 2026-08-13：完成目录、职责、类型、组件、状态、接口、样式、测试和质量门禁规范；明确从 `digitizing` 采用与排除的规则 | `c362020` |
| `P0-15` | 建立 Python 后端代码规范和未来 Go 演进边界 | 已完成 | 2026-08-13：完成模块、类型、接口、权限、事务、数据、Worker、SSE、测试和跨语言演进规范，并接入仓库执行入口 | `92a7f83` |
| `P0-16` | 建立工程规则自动执行、ADR 和 AI/RAG 安全基线 | 已完成 | 2026-08-13：建立统一验证入口、前后端架构检查、完整契约漂移与兼容检查、仓库安全检查、ADR 模板和 AI/RAG 威胁模型；版本治理进入阶段 1A/1D | `3fffe1a` |

## 4. 阶段 1 节点

阶段 1 的完整节点、依赖和门禁见 [阶段 1 实施计划](./stages/stage-1-plan.md)。`P1Q-01` 与 `P1Q-02` 已完成文件、公开接口、字段语义和长函数内部阶段注释治理，后续代码直接执行增强后的注释门禁；阶段 1 已由 `stage-1-complete` 标签冻结。

| 节点 | 交付目标 | 状态 | 验收证据 | Git 提交 |
| --- | --- | --- | --- | --- |
| `P1A-01` | 冻结阶段 1 核心实体与接口增量，建立 `ReleaseManifest` Schema、兼容矩阵和生成骨架 | 已完成 | 2026-08-13：核心实体兼容性新增；发布清单专项测试 `18/18`、统一门禁 pytest `104/104`；前端构建、Ruff、mypy、架构、契约兼容和漂移检查通过 | `bbbd859` |
| `P1A-02` | 按 `ADR-001` 将 Redis 7.4 替换为固定 Valkey 8.x | 已完成 | 2026-08-13：固定 Valkey 8.1.5 amd64/arm64 镜像摘要；新旧目录隔离、AOF 停启恢复、专项测试 `26/26`、统一门禁 `108/108` 及七项容器诊断通过 | `faaa2a2` |
| `P1A-03` | 建立正式模块化单体目录、依赖装配、统一错误映射和 Migration 基线 | 已完成 | 2026-08-13：应用工厂、依赖生命周期、启动兼容门和错误目录映射通过；架构反例生效；随机 Schema `base → head → base → head` 结构一致；统一门禁 pytest `123/123` 及七项容器诊断通过 | `48b713a` |
| `P1A-04` | 建立账号认证、可信 `RequestContext`、本地会话与 API Key 加密边界 | 已完成 | 2026-08-13：Argon2id、Valkey Session、CSRF、逐请求成员复核、单向摘要 Open API Key、AES-256-GCM 信封加密和只读主密钥挂载通过；专项 `24/24`、真实基础设施 `5/5`、统一门禁 pytest `138/138` 及七项容器诊断通过 | `2e4cf92` |
| `P1A-05` | 建立审计事实、Outbox 发布调度、幂等消费和 Trace 跨 HTTP/任务/事件传播 | 已完成 | 2026-08-14：不可变审计、同事务 Outbox、租约认领、有限重试/死信、签名任务信封、重复消费和 Trace 延续通过；真实基础设施 `17/17`、统一门禁 pytest `155/155`；Migration、Celery 注册、真实合成事件发布消费和八项诊断通过 | `b14c4b6` |
| `P1A-06` | 固定契约生成器、Secret Scanner、镜像扫描策略和发布级供应链门禁 | 已完成 | 2026-08-14：固定 React/Python 契约类型生成与漂移门禁；当前文件和完整 Git 历史 Secret Scanner 通过；本地开发供应链门禁通过，正式发布因镜像扫描 `not_configured` 与 Linux `not_run` 明确阻断；`httpx2 2.10.0` 迁移完成；专项 `20/20`、统一门禁与八项容器诊断通过 | `f2d2a83` |
| `P1B-01` | 注册、登录、默认个人空间和个人空间所有者模型 | 已完成 | 2026-08-14：账号、唯一默认个人空间、所有者成员关系、审计与 Outbox 同事务创建；规范化重复登录名稳定返回 `409`；非所有者即使具有成员记录也无法访问个人空间；真实 HTTP 闭环、Migration 往返、统一门禁 pytest `167/167` 及八项容器诊断通过 | `eaf5011` |
| `P1B-02` | 企业空间、成员邀请、加入、离开、停用和空间切换 | 已完成 | 2026-08-14：唯一且不可移除的企业所有者、7 天定向邀请、成员停用/重新加入/离开、逐请求成员复核、聚合版本事件及审计/Outbox 同事务通过；Migration 往返、真实双账号 HTTP 闭环、统一门禁 pytest `175/175` 及八项容器诊断通过 | `bbb5398` |
| `P1B-03` | 多级部门树、成员多部门归属、主部门和岗位 | 已完成 | 2026-08-14：邻接树与闭包表、环和悬空父节点拒绝、层级移动、祖先停用继承关闭、岗位及成员归属通过；真实 PostgreSQL 专项 `3/3`、全量 pytest `183/183`、真实 HTTP 闭环及八项容器诊断通过 | `a8bf242` |
| `P1B-04` | 空间、部门和成员角色的确定性继承 | 已完成 | 2026-08-14：系统角色、三类绑定范围、部门祖先继承、多来源去重、成员自读、版本化 Valkey 缓存及成员生命周期撤权通过；真实 PostgreSQL/Valkey 专项 `3/3`、全量 pytest `191/191`、真实 HTTP 闭环及八项容器诊断通过 | `9ab81e2` |
| `P1B-05` | `Entitlement`、个人/企业套餐、配额、用量和空间状态 | 已完成 | 2026-08-14：版本化套餐快照、原子幂等用量、月度隔离、成员上限、空间停用、Open API 开关及旧 Key 即时失效通过；真实 PostgreSQL 专项 `3/3`、全量 pytest `197/197`、真实 HTTP 闭环及 Revision `20260814_0010` 八项容器诊断通过 | `e18f015` |
| `P1B-06` | 个人/企业空间切换、成员、组织和套餐管理页面 | 已完成 | 2026-08-14：注册登录、空间切换、创建企业、接受邀请、套餐/Open API、成员与组织治理页面通过；共享 UoW 并发隔离回归 `6/6`、统一门禁 pytest `204/204`、六类响应式视口、普通成员无权限状态及 Revision `20260814_0010` 八项容器诊断通过 | `6a7f2f6` |
| `P1C-01` | 建立 `Permission`、`Menu`、`PageResource`、`ApiResource` 和统一 `permission_code` 注册表 | 已完成 | 2026-08-14：版本化注册表覆盖 28 项 Permission、5 个页面、31 个 OpenAPI 操作和 5 个系统菜单；重复、悬空、未绑定、循环、停用引用与 OpenAPI 漂移门禁通过；统一门禁 pytest `209/209`、React `3/3`、真实导航及八项容器诊断通过 | `087d8ac` |
| `P1C-02` | 实现 RBAC、资源/数据级 ABAC 和统一策略决策点 | 已完成 | 2026-08-14：持久化 RolePermissionGrant 与工作空间、部门树、本人、资源四类范围；33 个 OpenAPI 操作统一强制授权，策略故障默认拒绝，Open API Key Scope 只缩小权限；真实部门范围过滤与 URL/API 绕过拒绝通过；统一门禁 pytest `217/217`、React `3/3`、Revision `20260814_0011` 八项容器诊断通过 | `1faad49` |
| `P1C-03` | 实现字段级 ABAC、响应字段掩码和敏感级别规则 | 已完成 | 2026-08-14：四级密级、显式字段遮罩和字段策略注册表进入统一 PDP；成员响应、运行日志、检索/引用和模型上下文在出口前投影，受限字段不进入序列化；Migration 往返与真实 PostgreSQL 字段授权通过；统一门禁 pytest `225/225`、React `3/3`、Revision `20260814_0012`、八项容器诊断和真实 HTTP 验收通过 | `708c57a` |
| `P1C-04` | 实现自定义菜单与页面、接口统一绑定 | 已完成 | 2026-08-14：32 项 Permission、37 个 OpenAPI 操作、35 个菜单资源与 30 个动作接口绑定统一注册；个人/企业空间可配置已注册菜单名称、图标、排序、父子关系与可见性，角色可独立裁剪菜单；任意资源、循环、悬空和权限码不一致被拒绝，后端 PDP 仍独立授权；统一门禁 pytest `234/234`、React `3/3`、Revision `20260814_0013`、八项容器诊断及个人/企业真实 HTTP 闭环通过 | `767bf39` |
| `P1C-05` | 建立菜单草稿、校验、审批、不可变发布快照与版本回滚 | 已完成 | 2026-08-14：`draft → validated → approved/rejected → published` 状态机、SHA-256 摘要、当前发布指针和复制快照式回滚通过；个人空间两版发布后回滚、企业独立发布、普通成员只读当前快照及跨空间拒绝完成真实 HTTP 验收；Migration 往返 `2/2`、统一门禁 pytest `243/243`、React `3/3`、Revision `20260814_0014` 和八项容器诊断通过 | `925fa20` |
| `P1C-06` | 建立动态 React 应用壳层、路由守卫和个人/企业菜单裁剪 | 已完成 | 2026-08-14：当前发布快照、有效角色和静态页面注册表驱动导航；刷新、深链、无权限、加载失败、无发布兼容回退和移动抽屉通过；React `6/6`、Python 全量 `243/243`、生产构建、Migration 往返 `1/1`、菜单发布回归 `1/1`、Revision `20260814_0015`、八项容器诊断和真实桌面浏览器验收通过 | `45a5c32` |
| `P1D-01` | 建立知识库、文档、不可变文档版本、来源和当前发布事实模型 | 已完成 | 2026-08-14：五张工作空间隔离事实表、`draft → ready → published → superseded` 状态机、当前发布原子切换、软删除、部门范围、字段投影、配额用量及审计/Outbox 同事务通过；统一门禁 React `6/6`、Python `259/259`，Migration 往返、PostgreSQL/Valkey 回归 `6/6`、Revision `20260814_0016`、八项容器诊断和全合成 HTTP 闭环通过 | `9dead1c` |
| `P1D-02` | 建立可替换 S3 Adapter、受控对象上传、类型/大小校验和安全扫描 | 已完成 | 2026-08-14：20 MiB 可配置上限、九类首期扩展名、真实签名校验、确定性扫描、服务端工作空间对象键、扫描失败关闭、事实失败补偿删除和存储配额同事务通过；统一门禁 React `6/6`、Python `269/269`，专项 `60/60`、真实 MinIO、Migration 往返、Revision `20260814_0017`、八项容器诊断和真实 multipart HTTP 闭环通过 | `8715f6b` |
| `P1D-03` | 建立 `IngestionJob`、解析产物、中文 OCR Adapter、有限重试和失败定位 | 已完成 | 2026-08-14：上传事实与任务同事务创建；PostgreSQL 租约认领、`SKIP LOCKED`、最多 3 次指数退避、过期恢复和脱敏失败事实通过；Markdown 确定性解析与 Tesseract `chi_sim+eng` 合成中文 OCR 真实闭环通过；统一门禁 React `6/6`、Python `276/276`，Migration 回填与往返 `2/2`、Revision `20260814_0019`、八项容器诊断、三个 Celery 任务注册和历史 clean 上传 `3/3` 任务回填通过 | `ac9e83b` |
| `P1D-04` | 建立权限元数据 Chunk、Embedding、关键词索引和索引版本切换 | 已完成 | 2026-08-14：构建期 Chunk 不可见；首次发布、新版本先发布后建索引、同版本重建和文档删除均通过 PostgreSQL 原子切换或撤权；Chunk 保存部门、密级、权限标签、来源、任务和解析摘要；统一门禁 React `6/6`、Python `283/283`，专项单元 `6/6`、PostgreSQL 生命周期 `1/1`、Migration 往返 `2/2`、Revision `20260814_0020`、八项容器诊断、四个 Celery 任务注册和本地历史 Artifact 索引 `3/3` 通过 | `052e9e1` |
| `P1D-05` | 建立模型供应商配置、自定义 `base_url`、加密 Key、能力探测和数据政策 | 已完成 | 2026-08-14：独立平台管理员上下文及本地唯一提权入口、供应商配置、版本化信封加密凭证、平台审计、数据政策和能力探测通过；公网 HTTPS 白名单、DNS/IP 钉住、不跟随重定向及敏感级别外发门禁生效；统一门禁 React `6/6`、Python `289/289`，PostgreSQL 生命周期、Migration 往返、Revision `20260814_0021`、八项容器诊断和撤权即时生效 HTTP 闭环通过 | `0a5a86e` |
| `P1D-06` | 建立主备模型网关与不可变 `AiRuntimeConfigVersion` | 已完成 | 2026-08-14：运行配置冻结 Prompt、主备路由、价格、超时、重试、熔断、成本预算及知识运行组件版本；唯一调用先占位，逐次尝试、Token、成本、Trace、配置和凭证版本可追溯，重复调用不触发供应商；三个后置接口只冻结版本；统一门禁 React `6/6`、Python `299/299`，专项单元 `8/8`、PostgreSQL 闭环、Migration 往返、Revision `20260814_0022`、八项容器诊断和撤权即时生效 HTTP 闭环通过 | `626a27c` |
| `P1D-07` | 建立知识生产与模型配置页面 | 已完成 | 2026-08-14：知识库、文档、新版本、任务轮询、人工重试和发布状态闭环，以及平台供应商与不可变运行配置治理页面完成；解析任务与文档读模型自动同步，普通企业成员拒绝、个人所有者全合成 V1/V2 发布、平台管理员授予/撤销即时生效通过；统一门禁 React `11/11`、Python `306/306`，PostgreSQL/Migration `7/7`、Revision `20260814_0023` 和八项容器诊断通过 | `73a3637` |
| `P1D-08` | 补齐系统所有者知识权限种子并回填现有空间 | 已完成 | 2026-08-14：统一领域种子补齐知识页面、知识库、文档、入库读取和重试 5 项权限；Revision `20260814_0024` 幂等回填历史个人/企业所有者且降回 `0023` 保留兼容授权；新注册空间与历史空间 PostgreSQL/Valkey 回归 `6/6`、统一门禁 React `19/19`、Python `307/307`、八项容器诊断及 390px 真实知识页面恢复通过 | `09b652d` |
| `P1Q-01` | 完成前后端注释可维护性治理 | 已完成 | 2026-08-14：采用 `digitizing` 分层阅读思路，统一文件职责、导出/字段 JSDoc、Python 公开接口 docstring、执行边界业务契约和复杂流程编号注释；TypeScript/Python 注释门禁及反例测试接入统一验证，前端门禁 `5/5`、后端门禁 `6/6`、React `21/21`、Python `313/313`、格式、类型、架构、契约、供应链和生产构建全部通过 | `51a443c` |
| `P1Q-02` | 完成长函数内部注释治理 | 已完成 | 2026-08-14：固定 30/60/100 有效代码行分级规则，嵌套函数独立检查；TypeScript/Python 门禁正反例各 `13/13`、React `21/21`、Python `320/320`，格式、Lint、类型、构建、架构、契约、供应链及真实基础设施集成全部通过 | `cb71ed0` |
| `P1E-01` | 建立会话、不可变消息与系统知识助手发布快照 | 已完成 | 2026-08-15：个人与企业空间均支持创建者私有会话、不可变 `MessagePart`、系统 `AgentRelease`、幂等消息提交及唯一活动 Run 排队；Run 冻结模型运行配置版本，审计、Outbox、RBAC 和菜单接口绑定同边界接入；PostgreSQL/HTTP `2/2`、Migration 与联合回归 `25/25`、统一门禁 React `21/21`、Python `322/322`、Revision `20260815_0026` 和八项容器诊断通过 | `4636658` |
| `P1E-02` | 权限前过滤、查询分类/改写、有限多查询和混合检索 | 已完成 | 2026-08-15：共享确定性 Embedding Adapter、最高授权密级上下文、查询分类与 NFKC 规范化、最多 3 个变体/6 次通道操作/20 个最终候选/2 秒预算、授权索引前过滤、RRF 融合和不可变计划/变体/候选快照完成；跨空间、私有文档、部门范围、已删除文档和密级候选在模型前收敛，候选不保存正文且字段掩码后再入快照；单元专项 `6/6`、PostgreSQL 专项 `1/1`、Migration 往返 `3/3`、既有检索回归 `5/5`、统一门禁 React `21/21`、Python `329/329`、mypy strict `354` 个源文件、Revision `20260815_0027` 和八项容器诊断通过 | `9de3e2e` |
| `P1E-03` | 重排、来源排序、受控精读与引用验证 | 已完成 | 2026-08-15：确定性 `deterministic-lexical-reranker-v1`、`source-priority-v1` 来源排序、FastPass 重排跳过边界、候选/文档/Chunk/字符/Token/引用/耗时预算、当前版本与授权复核、不可变 `retrieval_evidence_sets/items` 和冲突降级完成；撤权、旧版本、Hash 漂移、伪造/缺失/不足证据与重复读取失败关闭；单元专项 `7/7`、PostgreSQL 专项 `1/1`、P1E-02 联合回归 `2/2`、Migration 往返 `3/3`、统一门禁 React `21/21`、Python `337/337`、mypy strict `364` 个源文件、Revision `20260815_0028` 重建启动成功；后续在同一 Revision 补验八项容器诊断通过 | `f188278` |
| `P1E-04` | 建立 Prompt Injection、投毒、泄漏和模型越权输出防护 | 已完成 | 2026-08-15：固定 `rag-safety-v2`，在检索前拒绝直接注入和查询改写越权，在字段投影后检查并隔离非可信证据，在模型出口拒绝授权决定、审批字段、敏感标记和外部工具参数；拒绝事实不保存模型正文，旧安全版本不能创建运行配置；`p0-11-v1` 安全样本、Unicode/Base64 混淆和完整调用边界专项 `14/14`，统一门禁 React `21/21`、Python `353/353`、mypy strict `366` 个源文件、Revision `20260815_0028` 和八项容器诊断通过 | `6e11901` |
| `P1E-05` | 建立 HTTP SSE、心跳、事件持久化和 `Last-Event-ID` 回放 | 已完成 | 2026-08-15：Run 单次认领、冻结运行配置调用、助手消息终态、短事务事件存储、15 秒心跳、24 小时保留、5000 事件/10 MiB 回放预算、游标后补发及无未确认事件时的 `message.snapshot` 完成；快照不设置 SSE `id`，不会污染下一次持久化游标；`p0-11-v1` 重连与预算样本、执行器、P0-10 回归及 PostgreSQL/HTTP 专项 `20/20`，统一门禁 React `21/21`、Python `358/358`、mypy strict `370` 个源文件、Revision `20260815_0029` 和八项容器诊断通过 | `6f0124e` |
| `P1E-06` | 完成问答、来源、流式恢复、取消和反馈页面闭环 | 已完成 | 2026-08-15：个人空间新账号可由仅限 `local/test` 的固定内置 Mock Provider 幂等自举当前运行配置，真实配置不被覆盖且真实草稿未发布时失败关闭；问答会话、流式恢复、Run 取消、来源权限/版本/索引复核及可修订反馈闭环完成，反馈评论不进入审计或 Outbox；真实浏览器完成首次问答、刷新恢复、来源降级、正负反馈、取消入口和 390×844 移动端验收；React `28/28`、Python `366/366`、mypy strict `377` 个源文件、PostgreSQL 专项 `10/10`、Revision `20260815_0030` 和八项容器诊断通过 | `2175d3e` |
| `P1F-01` | 建立工作流草稿、不可变版本、发布和运行事实模型 | 已完成 | 2026-08-15：确定性 DAG 校验拒绝循环、悬空边、不可达节点、无结果路径及超过 100 节点/200 边的图；发布版本和历史运行冻结且数据库触发器拒绝版本更新或删除，旧版本不能创建新运行；运行读取按自身资源复核范围并执行字段级 ABAC；统一门禁 React `28/28`、Python `376/376`、mypy strict `391` 个源文件，Migration 往返、PostgreSQL 不可变性、跨空间与 HTTP 认证闭环、Revision `20260815_0031` 和八项容器诊断通过；工作流页面入口保留停用，等待 `P1F-05` 实现后启用 | `ad51fca` |
| `P1F-02` | 完成 Trigger、Condition、Knowledge Retrieval、Model、Approval 和 Result 受限节点执行器 | 已完成 | 2026-08-15：固定 `workflow-executor-v1` 按冻结 DAG 确定性执行并持久化 Step/Attempt，条件认领拒绝重复执行；每个活动节点重新执行 PDP，知识正文追加 `knowledge.document.read` 复核，模型只经冻结运行配置、安全上下文和模型网关调用；声明式配置拒绝脚本及外部写操作，100 步、5 次模型、5 次检索、30 秒、单步 64 KiB 和总输出 256 KiB 预算在调用前后失败关闭；审批节点只进入 `waiting_approval`；统一门禁 React `28/28`、Python `383/383`、mypy strict `397` 个源文件，Migration 往返、PostgreSQL 状态事实、真实认证 HTTP 成功分支、Revision `20260815_0032` 和八项容器诊断通过；页面继续停用至 `P1F-05` | `4ed4b96` |
| `P1F-03` | 建立多级审批策略、不可变版本和确定性审批链计算 | 已完成 | 2026-08-15：最多 5 级串行审批且同级支持 `any/all`，审批人可来自指定账号、角色、部门负责人和上级负责人；资源、操作、部门、密级、风险、字段及金额条件参与确定性匹配，策略按优先级和特异度排序，同分冲突、无审批人和越权条件失败关闭；企业默认禁止申请人自审，个人空间固定生成所有者确认；提醒、超时、升级、转交、驳回、等待和候补配置进入不可变版本，策略与审批链生成稳定摘要；统一门禁 React `28/28`、Python `397/397`、mypy strict `406` 个源文件，专项单元 `12/12`、PostgreSQL `2/2`、Migration 往返 `3/3`、真实认证个人空间 HTTP 预计算、Revision `20260815_0033` 和八项容器诊断通过 | `195d696` |
| `P1F-04` | 建立审批实例、层级、指派、通过、驳回、转交、撤回、到期动作和工作流恢复 | 已完成 | 2026-08-15：审批运行聚合冻结策略版本与摘要，支持 `any/all` 多级串行推进、动作幂等、实例乐观锁、参与者可见性、通过/驳回/转交/撤回、提醒及 `wait/transfer/escalate/reject` 超时；动作事实只追加，审批终态与 Workflow Step/Run、审计和 Outbox 同事务提交，通过后按历史 Step 与累计预算恢复且不重复执行节点；统一门禁 React `28/28`、Python `410/410`、mypy strict `414` 个源文件，状态机 `9/9`、PostgreSQL/HTTP `4/4`、联合回归 `6/6`、Migration 往返 `3/3`、真实认证个人空间 HTTP 闭环、Revision `20260815_0034` 和八项容器诊断通过 | `5d2ee37` |
| `P1F-05` | 完成工作流设计、发布、运行、待办和审批页面 | 已完成 | 2026-08-15：工作流工作台支持六类节点编排、连线、JSON 配置、校验、保存和发布，运行监控支持创建、状态轮询、预算与字段级 ABAC 输入输出，审批区域支持策略创建/修订、待办、通过、驳回、转交和撤回；Registry 15 启用正式页面及接口绑定并为已有空间复制新菜单快照，历史快照保持不变；统一门禁 React `31/31`、Python `411/411`、mypy strict `415` 个源文件，Migration 往返与历史快照 `4/4`、个人/企业真实浏览器权限闭环、1440×900 与 390×844 响应式验收、Revision `20260815_0035` 和八项容器诊断通过 | `c1d16fa` |
| `P1S-00` | 冻结 UnoCSS 主样式体系、迁移边界和建设顺序 | 已完成 | 2026-08-14：接受 `ADR-003`；同步前端规范、UI/UX 基线、总体设计和阶段计划；明确 UnoCSS、CSS Token、Ant Design Theme、必要复杂 CSS、静态扫描和五个迁移节点边界；文档链接与差异检查通过 | `31cd699` |
| `P1S-01` | 接入 UnoCSS 工具链、语义 Token、项目断点和扫描门禁 | 已完成 | 2026-08-14：固定 UnoCSS `66.7.5`、clsx `2.1.1`、Wind3/Preflight 关闭、六类断点、横屏 Variant、语义 Token 和 `cn()`；样式架构检查 `3/3`、React `12/12`、Python `306/306` 及统一门禁通过，未迁移页面 CSS 产物无变化 | `f89c81f` |
| `P1S-02` | 迁移公共组件与应用壳层 | 已完成 | 2026-08-14：AppShell 与四个公共组件迁入 UnoCSS，删除 5 个 CSS Module；补齐明暗表面、精确 720px 与移动横屏 Variant 及公共组件中文 JSDoc；React `14/14`、Python `306/306` 和统一门禁通过；六类视口、侧栏、抽屉、菜单跳转及 320px 顶栏验收通过 | `31bbc6c` |
| `P1S-03` | 迁移登录、状态、概览、成员和组织页面 | 已完成 | 2026-08-14：5 个普通页面迁入 UnoCSS 并删除对应 CSS Module；补齐精确 560/820/1024px Variant、运行状态语义 Token 和方向性边框修复；桌面、390px 移动端及 820/821px 临界断点验收通过，组织表格滚动限制在局部容器；React `17/17`、Python `306/306` 和统一门禁通过 | `0ce4ab0` |
| `P1S-04` | 迁移知识生产与模型治理复杂页面 | 已完成 | 2026-08-14：知识生产和模型治理页面迁入 UnoCSS 并删除最后 2 个 CSS Module；补齐精确 600/900px Variant、语义 Token、无 Preflight 边框和活动态优先级；知识页 1440/821/820/390px、模型页 901/900/601/600/390px 及移动表格局部滚动和运行配置弹窗单列验收通过；React `19/19`、Python `306/306` 和统一门禁通过 | `1f8920e` |
| `P1S-05` | 清理样式遗留并完成全页面视觉验收 | 已完成 | 2026-08-14：CSS Module 归零，间距 Utility 统一映射语义 Token；门禁新增 CSS Module、无消费者 Token 和无显式样式边框拦截；统一门禁 React `21/21`、Python `307/307`、样式反例 `6/6` 和八项容器诊断通过；全页面多视口验收无溢出、缺失边框、无名控件或浏览器错误 | `3ff9a59` |
| `P1G-01` | 固定约 100 用户、7 部门、300～500 文档的版本化合成企业数据集 | 已完成 | 2026-08-15：固定 `p1g01-v1`，包含 100 个合成用户、7 个部门、360 份企业文档、4 份个人文档和 16 类个人/企业联合验收场景；生成器不依赖网络、数据库、系统时间、随机数或真实资料，固定产物摘要为 `f882d5bf…b313617`，凭证只允许后续本地装载器注入；专项 `8/8`、统一门禁 React `31/31`、Python `419/419`、mypy strict `417` 个源文件、Secret Scanner、生产构建、Revision `20260815_0035` 和八项容器诊断通过 | `a4a7ae6` |
| `P1G-02` | 个人/企业端到端、联合越权、SSE、审批、恢复和模型故障验收 | 已完成 | 2026-08-15：`p1g01-v1` 从空 Schema 迁移并装入 100 个账号、101 条成员关系、7 个部门、364 份文档、388 个版本和 324 个发布指针；版本化清单覆盖 16 类个人/企业场景、完整 `p0-11-v1`、稳定错误码和 Trace，联合验收 `33/33`；统一门禁 React `31/31`、Python `425/425`、mypy strict `422` 个源文件、生产构建、Revision `20260815_0035` 和八项容器诊断通过 | `40f5ba4` |
| `P1G-03` | 本地备份、恢复、导出、导入和数据库升级/回滚演练 | 已完成 | 2026-08-15：接受 `ADR-004`，`backup/restore` 与 `export/import` 共用 V1 `.aiprb` 流式 AES-256-GCM 恢复包，覆盖 PostgreSQL、MinIO 和文件型密钥并排除 Valkey/日志/运行时；真实备份确认对象引用 `10/10`、Revision `20260815_0035` 和 44 个 MinIO 物理文件，隔离干净数据库完成 Custom Dump 恢复及 `0034 → 0035` Migration 往返，主密钥事务性重包裹和对象快照恢复通过；专项 `12/12`、统一门禁 React `31/31`、Python `437/437`、mypy strict `427` 个源文件及八项容器诊断通过 | `7dfcf0d` |
| `P1G-04` | 全业务 UI/UX、响应式、可访问性和主色调评审 | 已完成 | 2026-08-15：八个正式页面在 `390×844` 下均保持单一 `h1`，六类桌面/平板/移动/横屏视口无页面级溢出；路由与跳转链接焦点、移动抽屉、中文可访问名称、`44×44px` 开关命中区、Readiness `503` 降级和平台管理员即时撤权闭环通过；森林绿与克制蓝绿色主题对比度达到 WCAG AA；统一门禁 React `36/36`、Python `437/437`、mypy strict `427` 个源文件、生产构建、Revision `20260815_0035` 和八项容器诊断通过 | `04d1908` |
| `P1G-05` | 运维手册、MVP 报告、ReleaseManifest、阶段关闭提交与标签 | 已完成 | 2026-08-15：本地 MVP 运维与故障手册、`0.1.0` 发布报告和真实本地镜像内容摘要 ReleaseManifest 已生成；清单摘要 `e8983e87…b62943` 与兼容矩阵检查通过；统一门禁 React `36/36`、Python `437/437`、mypy strict `427` 个源文件，联合验收 `33/33`、开发供应链和八项容器诊断通过；正式发布因镜像扫描 `not_configured`、Linux `not_run` 继续阻断；阶段标签 `stage-1-complete` 已创建 | `0d896a1` |

## 5. 阶段 2 节点

阶段 2 的完整节点、依赖和门禁见 [阶段 2 实施计划](./stages/stage-2-plan.md)，验证事实见 [阶段 2 验证报告](./stages/stage-2-report.md)，本地 `0.2.0` 发布事实见 [阶段 2 本地可靠性发布报告](./releases/stage-2-local-reliability/README.md)。`P2-01`～`P2-12` 已完成，阶段以 `stage-2-complete` 标签冻结。

| 节点 | 交付目标 | 状态 | 验收证据 | Git 提交 |
| --- | --- | --- | --- | --- |
| `P2-01` | 冻结可靠性不变量、SLO/保留期/传播时限、故障分类、指标契约和版本化故障注入数据集 | 已完成 | 2026-08-15：冻结 10 条可靠性不变量、10 项带事实来源/窗口/失败口径的指标、6 类保留策略、5 秒撤权传播和 8 类 12 个全合成故障场景；百分位至少 20 个样本，不足保持 `not_run`；专项与契约 `32/32`、统一门禁 React `36/36`、Python `446/446`、mypy strict `428` 个源文件、生产构建及八项容器诊断通过 | `9da73a9` |
| `P2-02` | 完成任务阶段、尝试历史、稳定终态、有限重试、超时、取消和人工恢复 | 已完成 | 2026-08-15：在既有 `ingestion_jobs` 事实源上新增阶段和不可变 Attempt 历史，支持 generation 隔离、最多 3 次人工恢复、稳定 `cancelled/timed_out` 终态及取消审计/Outbox 同事务；过期租约和同名 Worker 迟到结果均不能覆盖新执行，Attempt 终态更新/删除由数据库触发器拒绝；专项单元 `13/13`、PostgreSQL 与 Migration `6/6`，统一门禁 React `37/37`、Python `457/457`、mypy strict `431` 个源文件、契约兼容、生产构建、Revision `20260815_0036` 和八项容器诊断通过 | `08385b4` |
| `P2-03` | 隔离解析、OCR、Embedding、索引队列与 Worker 资源，建立死信、租约回收和安全并发调整 | 已完成 | 2026-08-15：独立 Scheduler 和 control/parsing/OCR/embedding/indexing 五个 Worker Lane 只消费固定队列，PDF/图片与普通解析分流，Embedding 与索引提交拆成不可见构建和原子发布；索引自动尝试耗尽进入稳定死信，人工恢复最多 3 代，迟到 Attempt 与并发重放不能重复发布；OCR 故障时该队列独立积压 11 条、其余队列为 0 且持续成功，恢复后积压归零；统一门禁 React `37/37`、Python `473/473`、mypy strict `433` 个源文件、生产构建、Revision `20260815_0037` 和 13 项容器诊断通过 | `0700701` |
| `P2-04` | 建立索引版本一致性巡检、全量重建、差异修复和原子切换 | 已完成 | 2026-08-15：PostgreSQL 持久化 8 类不含正文的索引差异；完整旧候选可原子恢复，无候选时先撤销异常活动面再从成功入库事实幂等重建；健康旧索引在完整提交前继续服务，失败构建不可见，恢复耗尽的不可见 Chunk 可清理；Scheduler 每 300 秒将巡检固定投递到 indexing Lane，真实任务落库 `inspection|1|0|0|0`；专项 `5/5`、统一门禁 React `37/37`、Python `480/480`、mypy strict `438` 个源文件、生产构建、Revision `20260815_0038` 和 13 项容器诊断通过 | `8fa9e90` |
| `P2-05` | 建立跨实例 SSE 通知、连接恢复和消息快照回放，保持 PostgreSQL 事件事实 | 已完成 | 2026-08-15：接受 `ADR-005`，使用 Valkey Pub/Sub 按 Run 发布固定无正文载荷，提交后唤醒并强制回查 PostgreSQL；通知丢失或 Valkey 不可达时按默认 250 毫秒有界轮询，不重复创建 Run、不跨空间且不丢序号；独立实例中断替换、通知丢失、不可达和 20 个恢复样本专项 `4/4`，本地 nearest-rank `p99 <= 5s`；统一门禁 React `37/37`、Python `490/490`、mypy strict `441` 个源文件、生产构建、Revision `20260815_0038` 和 13 项容器诊断通过 | `0319abc`、`52dcf4b` |
| `P2-06` | 建立安全结构化日志、OpenTelemetry Trace、Prometheus 指标和分层健康/告警基线 | 已完成 | 2026-08-15：版本化字段注册表对日志、Span、指标和告警执行未登记即拒绝，凭据、正文、Prompt、Payload、受限字段和高基数资源标识不得导出；HTTP、五个 Worker Lane、检索、模型、Assistant、工作流、审批、SSE 与 Outbox 接入 Trace 和低基数指标，验签后任务才可恢复父 Trace；20 条完整关键 Trace 覆盖率 `1.0`，四通道敏感数据拒绝及多进程指标聚合通过；运行端点同时采集 API 与五个 Worker Lane，统一门禁 React `37/37`、Python `500/500`、mypy strict `447` 个源文件、真实 PostgreSQL/Valkey 专项 `18/18`、生产构建和 13 项容器诊断通过 | `9ea06f2` |
| `P2-07` | 建立审计、用量、配额和 Transactional Outbox 的运营查询、积压恢复、重放与幂等巡检 | 已完成 | 2026-08-15：审计补齐权限码、策略决策 ID 和策略版本，用量账本支持工作空间隔离分页与计数器/明细对账；Outbox 提供不含 Payload 的状态查询、积压与 Schema/消费幂等巡检，人工重放只允许具有独立重放权限的浏览器主体（默认仅 Owner）对 `published/dead_letter` 事件操作，保留原事件 ID 并将不可变请求、重新排队和审计同事务提交；重复消费只累计接收次数，不重复执行投影；资源注册表 16 含 78 权限、107 API、95 菜单和 100 绑定；专项 HTTP `2/2`、PostgreSQL `5/5`、Migration `4/4`、联合回归 `20/20`，统一门禁 React `37/37`、Python `508/508`、mypy strict `456` 个源文件、生产构建、Revision `20260815_0039` 和 13 项容器诊断通过 | `ac10043` |
| `P2-08` | 建立工作空间数据导出、清除、保留期执行和派生数据清理证明 | 已完成 | 2026-08-15：接受 `ADR-006`；新增确定性 `workspace-export.v1` ZIP/JSONL、表和对象摘要校验、工作空间表强制分类、业务数据清除、MinIO/Valkey 清理及不可变删除证明，治理壳层与最小保留事实继续存在；24/30/90/365 天保留策略、Owner 独立权限、三项 HTTP 接口和并发幂等控制完成；专项 `8/8`、Migration 往返及联合回归通过，统一门禁 React `37/37`、Python `516/516`、mypy strict `475` 个源文件、生产构建、Revision `20260815_0040` 和 13 项容器诊断通过，真实合成个人空间 HTTP 导出、保留期和清除均为 `completed` | `78fbf2c` |
| `P2-09` | 建立策略缓存失效、权限变更传播、越权回归和异常访问告警 | 已完成 | 2026-08-15：PostgreSQL `role_version` 保持唯一策略版本事实，Valkey 仅保存原子比较且可重建的版本见证；缺失见证自举，一致版本放行计算，旧版先刷新但当前请求失败关闭，未来版、损坏和不可用均拒绝并统一返回可重试 `503 POLICY_UNAVAILABLE`；菜单、API、检索和字段投影四表面完成 20 个撤权样本，nearest-rank `p99 <= 5s` 且无受限内容暴露；React 每 2 秒刷新菜单与有效角色，异常时清空旧导航；低基数授权指标和三类 Prometheus 告警不包含主体、空间或资源标识；专项 `10/10`、真实 PostgreSQL/Valkey `1/1`、联合回归 `26/26`，统一门禁 React `38/38`、Python `529/529`、mypy strict `478` 个源文件、生产构建、Revision `20260815_0040` 和 13 项容器诊断通过 | `fbbae62` |
| `P2-10` | 建设任务、索引、Outbox、指标、生命周期和恢复操作的受控运营工作台 | 已完成 | 2026-08-16：Registry 18 将入库取消、索引巡检、全量重建和失败索引清理纳入统一菜单/API 权限；工作台聚合任务、索引请求与完成证据、Outbox、审计、用量和生命周期历史，危险命令执行浏览器主体、固定确认词、原因码、幂等、审计和 Outbox 门禁；索引 Worker 使用请求 ID 形成稳定运行证据，失败最多重试 3 次后进入死信；专项单元 `4/4`、PostgreSQL `3/3`，统一门禁 React `42/42`、Python `536/536`、mypy strict `495` 个源文件、生产构建、Revision `20260815_0041` 和 13 项容器诊断通过；`1440×900` 与 `390×844` 真实浏览器完成巡检闭环、无页面级横向溢出且宽表格只在局部滚动 | `0f20d17` |
| `P2-11` | 完成数据库、对象、索引、Valkey、Worker、API 实例、SSE 和 Outbox 联合故障演练 | 已完成 | 2026-08-16：冻结 `p2-11-v1` 12 场景清单及清单/证据 Schema，`./platform drill-stage-2` 执行前后 13 项诊断、单次 pytest/JUnit 和本地原子证据；真实演练首次发现并修复平台环境变量覆盖隔离恢复库的 Migration 回归，干净提交 `878f7ef` 上最终 `12/12` 通过、两轮诊断通过、证据 `repository_dirty=false`；统一门禁 React `42/42`、Python `543/543`、mypy strict `497` 个源文件及生产构建通过 | `878f7ef` |
| `P2-12` | 完成阶段 2 端到端验收、运维更新、ReleaseManifest、阶段报告、关闭提交与标签 | 已完成 | 2026-08-16：`0.2.0` ReleaseManifest 固定 Revision `20260815_0041`、运行时、源码和 7 个本地镜像摘要，自摘要 `17ae80ee…7b245d`；统一门禁 React `42/42`、Python `544/544`、mypy strict `497` 个源文件，13 项容器诊断、联合故障演练 `12/12`、开发供应链和 `1440×900`/`390×844` 浏览器门禁通过；正式发布因镜像扫描 `not_configured`、Linux `not_run` 保持阻断；阶段标签 `stage-2-complete` 已创建 | `7dfa517`、`763f51b`、`0e26300` |

## 6. 阶段 3 节点

阶段 3 的完整节点、依赖和门禁见 [阶段 3 实施计划](./stages/stage-3-plan.md)，验证事实见 [阶段 3 验证报告](./stages/stage-3-report.md)，本地 `0.3.0` 发布事实见 [阶段 3 本地 Agent 平台发布报告](./releases/stage-3-local-agent-platform/README.md)。`P3-01`～`P3-13` 已完成，阶段以 `stage-3-complete` 标签冻结。

| 节点 | 交付目标 | 状态 | 验收证据 | Git 提交 |
| --- | --- | --- | --- | --- |
| `P3-01` | 冻结 Agent 控制面领域契约、状态机、安全不变量、权限菜单增量边界和版本化合成验收集 | 已完成 | 2026-08-16：新增 `agent-control.v1` 核心事实 Schema 与 Golden Fixture，冻结 Agent、草稿、候选、不可变 Release、Service、版本化 Route 和 Run 唯一绑定；`p3-01-v1` 基线固定 3 个状态机、10 条安全不变量、个人所有者/企业多级审批模式、19 个预留权限、15 个预留菜单、19 个计划 API Operation、8 类事件和 8 项后置能力，预留项未提前进入活动资源注册表；17 个全合成场景覆盖核心边界；专项契约 `38/38`，统一门禁 React `42/42`、Python `558/558`、mypy strict `498` 个源文件、生产构建及 13 项容器诊断通过 | `88a2a2f` |
| `P3-02` | 建立 `Agent`、`AgentDraft`、发布候选和不可变 `AgentRelease` 生命周期事实 | 已完成 | 2026-08-16：新增工作空间隔离的当前草稿、不可变 revision、发布候选和幂等请求事实，扩展共享 Agent/Release 的 `system/custom` 边界；创建、乐观锁更新、候选冻结、归档、并发重放、跨空间拒绝及审计/Outbox 同事务通过；数据库拒绝 Release、历史和候选来源篡改，存在自定义数据时拒绝不安全降级；专项单元 `3/3`、PostgreSQL `4/4`，Migration/系统助手/生命周期联合 `13/13`；统一门禁 React `42/42`、Python `565/565`、mypy strict `515` 个源文件、生产构建、相对 `stage-2-complete`/`HEAD` 契约兼容、Revision `20260816_0042` 和 13 项容器诊断通过 | `6339144` |
| `P3-03` | 建立 Prompt、模型、知识库、工作流和只读工具的草稿配置与校验 | 已完成 | 2026-08-16：新增禁止额外字段的严格 Agent 配置模型，固定 Prompt、全局已发布模型、知识范围、工作流发布版本、只读工具、输出 Schema、`rag-safety-v2` 和预算引用；配置在创建、草稿更新和候选申请时统一复核，失效、跨空间、越权、重复引用、写工具和明文凭证均失败关闭；Prompt、知识范围和输出 Schema 形成内容寻址的不可变版本，配置摘要排序后稳定复算；Revision `20260816_0043` 新增六类不可变配置事实和五个固定只读工具；专项单元 `11/11`、P3-02/P3-03 联合单元 `14/14`、PostgreSQL `8/8`、Migration/生命周期/发布治理 `17/17`；统一门禁 React `42/42`、Python `580/580`、Ruff 与 mypy strict `522` 个文件、生产构建、公共数据库升级和 13 项容器诊断通过 | `773d2b9` |
| `P3-04` | 建立固定测试集、规则/指标评估、安全测试和不可变测试结果 | 已完成 | 2026-08-16：新增内容寻址的固定测试集、五类必需检查、确定性策略和不可变运行结果；失败、超时和跳过统一计为失败，越权、提示注入、引用和输出契约为不可抵消硬门禁；可信内部执行器、幂等重放、工作空间隔离、候选状态推进及数据库防绕过约束通过；专项单元 `5/5`、P3-02～P3-04 联合单元 `19/19`、PostgreSQL/Migration 联合 `17/17`、Migration 往返 `4/4`；统一门禁 React `42/42`、Python `590/590`、Ruff 与 mypy strict `528` 个源文件、生产构建、Revision `20260816_0044` 和 13 项容器诊断通过 | `4dbc736` |
| `P3-05` | 复用多级审批完成个人所有者与企业发布审批门禁 | 已完成 | 2026-08-16：发布审批复用阶段 1 既有审批运行时，个人空间使用固定内置策略执行所有者一级确认并允许自批，企业空间继承最多五级策略、自审限制、转交、提醒和超时动作；不可变审批绑定冻结候选与配置摘要、评估运行与结果摘要、评估策略、审批实例、审批策略及 Subject/Chain 摘要；新草稿 revision 自动令旧候选失效，历史审批可结束但不能恢复或发布失效候选；生命周期扩展点保证审批、候选、审计和 Outbox 同事务，数据库 Trigger 拒绝伪造状态与篡改绑定；专项 PostgreSQL `4/4`、审批及 P3-02～P3-05 联合 `27/27`、Migration 往返 `4/4`、相邻单元/架构/注释回归 `42/42`；统一门禁 React `42/42`、Python `594/594`、Ruff 与 mypy strict `533` 个源文件、生产构建、Revision `20260816_0045` 和 13 项容器诊断通过 | `89575ae` |
| `P3-06` | 生成可验证、不可变且绑定审批和测试证据的发布快照 | 已完成 | 2026-08-16：自定义 `AgentRelease` 从不可变草稿 revision 构造规范快照，固定配置、测试集版本、五类通过检查、评估策略、审批实例和审批策略证据；快照摘要在写入和读取时复算，Release、候选终态、幂等请求、审计和 Outbox 同事务提交；Agent/候选固定锁顺序和候选唯一约束保证重复请求只产生一个版本；四类来源复合外键、插入校验 Trigger、候选终态 Trigger 和既有不可变 Trigger 拒绝缺少证据的直接插入、直接推进、更新和删除；时间证据按 `timestamptz` 语义比较并拒绝额外字段；专项 PostgreSQL `3/3`、P3-06/Migration `7/7`、P3-02～P3-06 与 Migration 联合 `43/43`；统一门禁 React `42/42`、Python `597/597`、Ruff 与 mypy strict `536` 个源文件、生产构建、Revision `20260816_0046` 和 13 项容器诊断通过 | `1d3bec3` |
| `P3-07` | 建立 `Service`、`ServiceRoute`、访问策略和服务状态治理 | 已完成 | 2026-08-16：接受 `ADR-007`，独立服务治理模块独占 Service、版本化策略、不可变 Route、当前指针和幂等请求写入；自定义服务原子激活、策略版本、暂停/恢复/归档、跨空间与跨 Agent 防绕过、系统助手双指针兼容和历史 Run 不漂移通过；系统 Route 同步推进 Service 版本保证 Outbox 顺序，Migration 只回填固定 `system_knowledge` 助手并保留其他系统 Agent；专项单元 `4/4`、PostgreSQL `3/3`、Migration `5/5`、联合回归 `60/60`；统一门禁 React `42/42`、Python `605/605`、mypy strict `552` 个源文件、生产构建、Revision `20260816_0047` 和 13 项容器诊断通过 | `2cf0de8` |
| `P3-08` | 建立只读取已发布快照的 Runtime 路由与控制面故障隔离 | 已完成 | 2026-08-16：接受 `ADR-008`，独立 Runtime 模块只读取 Service、当前或精确 Route、AgentRelease 和 Agent 发布事实，拒绝草稿、候选、审批当前态、失效状态、摘要损坏和 P3-09 前的灰度 Route；当前 Route 使用 300 秒短租期缓存，精确 Run 绑定使用 86400 秒长租期缓存，规范信封摘要、身份错位删除回源、Source 故障续跑和双故障失败关闭通过；新 Run 在数据库、审计和 Outbox 冻结 Service/Route/Route Version/Release 四元绑定，执行前再次复核，暂停服务拒绝新 Run，但在途 Run 可收敛，跨空间、历史 Route 新写入、Release 错配和改绑由数据库拒绝；Revision `20260816_0048` 仅回填唯一可证明的历史 Route，无法证明的历史 Run 保持空绑定且不能重执行，存在绑定时拒绝降级；专项单元 `12/12`、助手执行器 `1/1`、PostgreSQL `2/2`、Migration `6/6`，统一门禁 React `42/42`、Python `620/620`、mypy strict `564` 个源文件、生产构建和 13 项容器诊断通过 | `328bcca` |
| `P3-09` | 建立灰度、正式切换、一键回滚和并发发布控制 | 已完成 | 2026-08-16：接受 `ADR-009`，以 `SHA-256(service_id + assignment_key) % 100` 建立不保存原始分配键的固定百分位，Runtime 按 `canary` Route 稳定选择主版本或灰度 Release；灰度、晋级和回滚均只追加 Route，并在同一事务切换 publication、推进 Service 版本、保存幂等结果、审计和 Outbox，`expected_generation` 保证并发发布唯一胜者；current 缓存升级为每服务固定 100 桶，发布、服务状态和系统助手 Route 变化在提交后整体失效，在途 Run 继续按冻结 Route/Release 完成；Revision `20260816_0049` 扩展控制操作和 Run Trigger，拒绝跨空间、跨 Agent、失效 Release、非法灰度结构、历史 Route 新绑定和不安全降级；专项单元 `18/18`、P3-09 PostgreSQL `3/3`、相邻 PostgreSQL 与 Migration `11/11`，统一门禁 React `42/42`、Python `625/625`、Ruff 与 mypy strict `568` 个源文件、生产构建、公共数据库升级和 13 项容器诊断通过 | `6aa5a21` |
| `P3-10` | 交付自定义知识 Agent、场景应用和 Open API 服务出口 | 已完成 | 2026-08-16：接受 `ADR-010`，新增统一 `service_delivery` 调用链，自定义知识 Agent、场景应用和 Open API 复用访问策略、Runtime 路由、固定窗口限流、月度问答配额、隐藏调用会话、Assistant Run、HTTP 快照和可恢复 SSE；浏览器与 API Key surface 严格分离，服务级 Scope 精确收窄到单个 Service，API Key Actor 独立承担幂等、读取和审计归属，内部 RAG 恢复创建账号上下文并重新执行 RBAC/ABAC；Revision `20260816_0050` 回填会话类型和 Run Actor 并拒绝改绑或不安全降级，Registry 19 含 86 权限、122 API、105 菜单和 115 绑定；P3-10 单元 `14/14`、PostgreSQL/Valkey/HTTP/SSE `5/5`、Migration `7/7`，统一门禁 React `42/42`、Python `649/649`、mypy strict `582` 个源文件、生产构建、公共数据库升级和 13 项容器诊断通过 | `0822475` |
| `P3-11` | 建设 Agent 控制台、测试、审批、服务和发布回滚页面 | 已完成 | 2026-08-16：Registry 20 激活 Agent/服务控制台权限、16 个 API 绑定和个人/企业菜单发布快照，当前注册表共 103 项权限、138 个 API、123 个菜单和 131 个绑定；前端交付 Agent 草稿、候选、五类测试、个人/企业审批、Release 及服务策略、灰度、晋级、回滚和状态治理页面；创建 Agent 默认使用服务端生成的完整基础配置，同时保留完整 JSON 高级模式，本地 Mock 环境可显式自举 Runtime，非本地环境继续失败关闭；专项契约 `38/38`、真实 PostgreSQL HTTP `1/1`，统一门禁 React `50/50`、Python `652/652`、mypy strict `591` 个源文件、生产构建通过；Revision `20260816_0051` 和 13 项容器诊断通过；合成个人空间完成首个 Agent、两版草稿、两次候选、五类测试、所有者审批、两版 Release、服务创建、10% 灰度、晋级、回滚、暂停和恢复全链路，`1440×900` 与 `390×844` 真实浏览器无页面级横向溢出，移动端完整 JSON 弹窗完整位于视口内 | `a5e205b` |
| `P3-12` | 建立 AgentRelease 级质量、延迟、错误和成本运营视图 | 已完成 | 2026-08-16：新增脱敏只读运营聚合、主版本与灰度/上一版本对比、固定 `agent-operations-v1` 阈值、样本不足与 Route 身份漂移失败关闭、晋级证据摘要和告警；Registry 21 共 104 权限、139 API、125 菜单和 132 绑定，Revision `20260816_0052` 原子升级个人/企业所有者权限与菜单快照；前端交付服务选择、24/48/168 小时窗口、晋级结论、指标和告警页面，不返回 Prompt、正文、Run、Actor 或 Trace；专项 `11/11`、真实 PostgreSQL `1/1`、相邻联合回归 `5/5`，统一门禁 React `53/53`、Python `658/658`、mypy strict `605` 个源文件、生产构建和 13 项容器诊断通过；`1440×900` 与 `390×844` 无页面级横向溢出，两张宽表只在局部滚动，48 小时窗口 URL 恢复和浏览器零控制台错误通过；AI 质量与在线 LLM Grading 保持未配置 | `6ca4c51` |
| `P3-13` | 完成阶段 3 联合验收、ReleaseManifest、阶段报告、关闭提交与标签 | 已完成 | 2026-08-16：新增 `p3-13-v1` 15 场景清单、失败关闭执行器和最小证据 Schema；干净提交 `bbf5303` 上联合验收 `15/15`、前后两轮 13 项容器诊断通过且 `repository_dirty=false`；统一门禁 React `53/53`、Python `665/665`、mypy strict `607` 个源文件及生产构建通过；`0.3.0` ReleaseManifest 固定 Revision `20260816_0052`、运行时、源码和 7 个本地镜像摘要，自摘要 `60e5d17d…73e7c81`；开发级供应链通过，正式发布因镜像扫描 `not_configured`、Linux `not_run` 保持阻断；阶段标签 `stage-3-complete` 已创建 | `bbf5303`、`90eea09` |

## 7. 阶段 4 节点

阶段 4 的完整节点、依赖和门禁见 [阶段 4 实施计划](./stages/stage-4-plan.md)，最终事实见 [阶段 4 验证报告](./stages/stage-4-report.md)。`P4-01`～`P4-13` 已全部通过并由 `stage-4-complete` 标签冻结；P4-13 正式证据绑定干净修订 `62fac84a…`，十五场景 `15/15`、前后两轮 13 项诊断、最终镜像和双视口浏览器验收通过，统一门禁 React `58/58`、Python `835/835`、mypy strict `677` 个源文件及其余工程检查全部通过。

| 节点 | 交付目标 | 状态 | 验收证据 | Git 提交 |
| --- | --- | --- | --- | --- |
| `P4-01` | 冻结工具执行、任务状态、确认、幂等、取消、权限事件和版本化合成安全契约 | 已完成 | 2026-08-16：新增 `tool-execution.v1` 核心事实、`p4-01-v1` 冻结基线和 24 个全合成场景，固定 5 个状态机、12 条安全不变量、五个首批内部只读工具、只允许内部只读/合成内部写 Adapter、个人所有者确认、企业最多五级审批、先持久化幂等身份、结果未知人工恢复、取消终态优先、凭证调用边缘和工具结果四项安全检查；预留 6 权限、5 菜单、10 API 和 10 事件但未提前激活；新增 15 个稳定工具错误码和 ADR-011；专项 `58/58`、统一门禁 React `53/53`、Python `685/685`、mypy strict `608` 个源文件、生产构建和 13 项容器诊断通过；未创建数据库表、活动 API、菜单、凭证或真实 Adapter | `10b3627` |
| `P4-02` | 建立工具注册、不可变版本、参数/结果 Schema、风险和凭证需求治理 | 已完成 | 2026-08-16：原地扩展唯一 `agent_tool_definitions` 工具定义事实源，新增 Draft 2020-12 参数/结果 Schema 及 SHA-256、风险、Adapter、超时、重试、凭证需求、状态、合成标记和完整定义摘要；新增不可变 `tool_plan_availability`，五个冻结内部只读工具同时面向 `personal_local` 与 `enterprise_simulated`；工作空间目录严格取平台注册最新活动版本、当前套餐和当前 PDP 权限交集，历史 AgentRelease 仍按精确工具版本校验；数据库与应用共同拒绝定义或套餐映射改写、未知字段、开放 Schema、任意 HTTP Adapter、非法读写/重试组合、跨空间和摘要篡改；专项单元 `9/9`、PostgreSQL `3/3`、相邻与 Migration 联合回归 `45/45`，统一门禁 React `53/53`、Python `697/697`、mypy strict `620` 个源文件、生产构建和 13 项容器诊断通过，数据库 Revision 为 `20260816_0053`；本节点未激活 HTTP API、菜单、工具执行、凭证值或真实 Adapter | `43d099e` |
| `P4-03` | 建立 Run、Step、Attempt 和 ToolCall 状态事实、租约与数据库约束 | 已完成 | 2026-08-16：新增 `tool_runs`、`tool_steps`、`tool_attempts`、`tool_calls` 四张工作空间业务事实表和 Revision `20260816_0054`；Run 冻结工作空间、Actor/账号、Service、AgentRelease、预算、幂等键和请求摘要，Step 冻结顺序、工具版本和规范参数摘要，Attempt 采用追加式历史与租约代际，Worker 使用 `FOR UPDATE SKIP LOCKED` 领取，迟到结果不能覆盖取消/超时终态；应用与 PostgreSQL 双重校验状态转换、复合外键、顺序、终态、唯一写入权和取消优先级，跨空间读取/取消、伪造租约和同键异载荷失败关闭；工具参数正文不落库，四张表已纳入工作空间导出与业务清除 Registry `v4`；专项单元 `8/8`、PostgreSQL `4/4`、P4-01/P4-02/Migration 联合回归 `50/50`，统一门禁 React `53/53`、Python `709/709`、mypy strict `627` 个源文件、生产构建、架构/注释/UnoCSS/契约检查和 13 项容器诊断通过；公共本地数据库已升级到 Revision `20260816_0054`；未提前开放 HTTP API、菜单、真实 Adapter、凭证注入或真实外部工具 | `89a5b3a` |
| `P4-04` | 接入五个冻结内部只读工具与统一 Adapter 接口 | 已完成 | 2026-08-16：新增统一 `ToolAdapterService`、内部只读端口、五个封闭 Adapter 类型和责任模块函数装配入口，继续使用唯一工具目录解析精确版本；调用前只允许 `internal_read`、只读、无凭证、非合成工具并按 Draft 2020-12 输入 Schema、当前套餐/PDP 和资源范围失败关闭，空资源集合不放行；返回按输出 Schema、字段遮罩、256 KiB、敏感字段/凭证格式和中英文 Prompt Injection 检查，形成稳定 SHA-256 与四项检查回执；专项单元 `5/5`、真实 PostgreSQL 目录闭环 `1/1`，统一门禁 React `53/53`、Python `715/715`、mypy strict `632` 个源文件、生产构建和 13 项容器诊断通过，数据库保持 Revision `20260816_0054`；未开放 HTTP API、菜单、模型工具计划、任意 HTTP/SQL/文件系统或真实外部连接器 | `053998e` |
| `P4-05` | 建立模型工具意图、执行计划冻结、每步预算和当前策略复核 | 已完成 | 2026-08-16：新增严格候选工具意图解析、不可变 AgentRelease 工具允许列表投影和唯一 `ToolExecutionPlanningService`；每步重新校验当前套餐、精确工具版本、Draft 2020-12 参数 Schema、资源级 PDP 和 Run/工具预算，参数正文不落库；Revision `20260816_0055` 新增不可变 `tool_policy_decisions` 并冻结 Step 超时、尝试、结果大小和成本预算，数据库要求 Step 进入 `ready` 前存在身份、权限、参数摘要和评估时间完全匹配的当前允许证据，Run、全部 Step、策略证据和状态在单事务提交，任一步冲突整体回滚；Registry 升级为 `v5`；专项单元 `7/7`、PostgreSQL `3/3`、P4-02/P4-03/P4-05/Migration 联合 `11/11`，统一门禁 React `53/53`、Python `725/725`、mypy strict `638` 个源文件、生产构建和 13 项容器诊断通过，公共数据库升级至 Revision `20260816_0055`；未开放 HTTP API、菜单、凭证、写工具、任意 HTTP/SQL/文件系统或真实外部连接器 | `a1e608b` |
| `P4-06` | 复用审批引擎建立个人确认和企业高风险多级审批 | 已完成 | 2026-08-16：新增绑定 Workspace、Run、Step、工具版本、规范参数摘要和 PDP 版本的 `tool_confirmations`，个人空间固定所有者确认，企业空间复用版本化最多五级审批；审批实例、申请时 PDP、确认和 Run/Step 等待态同事务提交，批准后必须追加新的当前 PDP 证据才能恢复 `ready`；参数、工具、策略版本、资源范围、字段遮罩、撤权或有效期变化以 `tool_confirmation_invalidations` 追加失效且不覆盖原审批终态；Revision `20260816_0056`、Registry `v6` 和数据库 Trigger 共同拒绝确认篡改、旧 PDP 复用及破坏性降级；专项及相邻回归 `16/16`、Migration/生命周期/发布联合 `26/26`，统一门禁 React `53/53`、Python `730/730`、mypy strict `644` 个源文件、生产构建和 13 项容器诊断通过；未执行 Adapter，未开放 HTTP API、菜单、凭证或真实副作用 | `482a911` |
| `P4-07` | 建立工具凭证引用、授权、轮换与调用边缘安全注入 | 已完成 | 2026-08-16：新增与 Workspace、工具 ID 和精确版本绑定的 `tool_credentials`，使用 AES-256-GCM 信封加密、独立数据密钥和每次轮换生成的新 `credential_ref`；仅浏览器会话中的个人/企业活动所有者可管理，普通企业成员拒绝；ToolCall 只允许在 `proposed → authorized` 时一次性绑定当前活动引用，撤销或轮换后失败关闭；明文只在持有共享锁的 Adapter 回调期间存在，返回值、异常、审计、Outbox 和导出均禁止泄漏，轮换以排他锁等待已开始调用结束；Revision `20260816_0057`、Registry `v7`、主密钥重包裹和破坏性降级保护通过；PostgreSQL 专项 `9/9`，统一门禁 React `53/53`、Python `739/739`、mypy strict `649` 个源文件、生产构建及 13 项容器诊断通过；未开放凭证 HTTP API、菜单或真实连接器 | `f8d6eef` |
| `P4-08` | 建立合成内部副作用 Adapter、稳定幂等身份和执行前提交协议 | 已完成 | 2026-08-16：新增 `ToolSideEffectService`、唯一 `ToolSideEffectStore`、PostgreSQL Adapter、合成副作用 Adapter、`tool_idempotency_records`、`synthetic_tool_side_effects` 和 Revision `20260816_0058`；幂等身份固定为 Workspace、Run 与 Step 且跨 Attempt 稳定，请求摘要绑定工具版本、参数摘要和确认摘要，副作用发生前必须先持久化预留；同请求重放只产生一次副作用，同键异请求返回 `IDEMPOTENCY_CONFLICT`，提交后响应丢失可读取已提交结果，未提交未知结果进入 `outcome_unknown` 并禁止自动重放；成功时幂等终态、ToolCall、Attempt、Step、Run、审计和 Outbox 同事务收口，数据库 Trigger 独立复核当前 PDP、批准确认、租约、工具定义和写调用状态；参数正文、结果正文和凭证明文不进入幂等、副作用、审计或 Outbox；Registry 升级为 `v8`，普通事务禁止修改或删除历史，生命周期清除使用受控旁路；PostgreSQL 专项 `9/9`、Migration/生命周期/P4-02～P4-08 联合 `45/45`，统一门禁 React `53/53`、Python `748/748`、mypy strict `654` 个源文件、生产构建及 13 项容器诊断通过 | `443d22d` |
| `P4-09` | 建立 Worker 租约、超时、有限重试、取消、死信和人工恢复 | 已完成 | 2026-08-16：实现提交 `18937a8` 建立安全只读有限重试、租约续期与重启恢复、取消观察、迟到结果隔离、总截止时间、结果未知只读对账和最多三代人工恢复；统一门禁 React `53/53`、Python `758/758`、mypy strict `658` 个源文件及生产构建通过，公共数据库升级至 Revision `20260816_0059`；最终工作树镜像重建成功，`./platform doctor` 的 Web、API、MinIO、Tika、PostgreSQL、Revision、Valkey、5 个 Worker 和 Scheduler 共 13 项全部通过 | `18937a8` |
| `P4-10` | 建立工具结果安全、SSE 进度、审计、用量、成本和观测 | 已完成 | 2026-08-16：Revision `20260816_0060` 新增不可变安全结果、Attempt 唯一用量和 Run 连续游标进度事实，四项结果检查通过后才能进入模型上下文；Worker、副作用、取消、租约过期、总超时、迟到结果和人工恢复均原子写入运营事实，审计与 Outbox 不含参数、结果或凭证正文，Prometheus 只使用固定低基数标签；工作空间 Registry `v9`、可观测字段 Registry `v2`、非数据库专项 `70/70`、P4-08/P4-09/P4-10 PostgreSQL `19/19`、生命周期与 P4-10 `6/6`、统一门禁 React `53/53` 与 Python `768/768` 通过，最终镜像重建成功且 13 项容器诊断通过；浏览器 API 和 `streamToolRun` SSE 帧由 P4-11 统一激活 | `a7918f5` |
| `P4-11` | 建设工具目录、计划、确认审批、任务进度、取消和历史页面 | 已完成 | 2026-08-17：工具目录、Run 创建/列表/详情、确认/驳回/取消、可恢复 SSE、两类页面和 Revision `20260816_0061` 已实现；相邻联合回归 `24/24`、P4-01/P4-11 单元 `20/20` 通过。审批恢复后修正两处 Registry 22 历史断言，真实浏览器又发现并修正个人确认模式契约错配；最终统一门禁 React `58/58`、Python `772/772`、Ruff、mypy strict `673` 个源文件、契约、Registry、供应链和生产构建全部通过，最终镜像和 13 项诊断通过。可调整视口 Playwright 在 `1440×900` 与 `390×844` 下验证工具执行页、任务详情和危险确认无页面级横向溢出；浏览器主动中断首个已建连 SSE body 后自动重连，两次请求均携带 `Last-Event-ID: 5` 且页面未出现实时流错误 | `2e771d1` |
| `P4-12` | 完成重复投递、超时、响应丢失、撤权、取消和 Worker 重启联合演练 | 已完成 | 2026-08-17：实现提交 `817abd5`；在干净修订 `ea46c8a…` 原样执行正式入口，前后两轮 13 项 doctor、固定 8 场景/9 case、六类样本和零失败汇总通过；正式证据清单 SHA-256 为 `c7886e87…64d7604`，证据文件 SHA-256 为 `e8e094aa…806319`，独立 Schema 校验和 P4-12 验收器 `37/37` 通过；原样统一门禁 React `58/58`、Python `809/809`、mypy strict `675` 个源文件、生产构建及全部工程检查通过 | `817abd5` |
| `P4-13` | 完成阶段 4 端到端验收、ReleaseManifest、阶段报告、关闭提交与标签 | 已完成 | 2026-08-17：干净修订 `62fac84a…` 上十五场景 `15/15`、前后两轮 13 项诊断、最终镜像、桌面/移动浏览器和统一门禁通过；`0.4.0` ReleaseManifest 自摘要为 `ecacb54f…3f2190`，正式发布因镜像扫描与 Linux 验收未配置保持阻断 | `62fac84` |

## 8. 阶段 5 节点

阶段 5 的范围、依赖和门禁见 [阶段 5 实施计划](./stages/stage-5-plan.md)，持续验证事实写入 [阶段 5 验证报告](./stages/stage-5-report.md)。阶段 4 已关闭，`P5-01`～`P5-03`、`P5-05` 和 `P5-07` 已完成；`P5-04` 本地机制已在 `e821801` 提交，但真实供应商和足够授权样本未配置，因此保持受阻。`P5-05` 已在 `eaa58fa` 提交，原有审批服务阻塞由 P5-07 当前树统一门禁通过解除，但真实价格与供应商账单仍保持 `not_configured`，`P5-06` 不得提前启动。当前按依赖进入 `P5-08`；真实供应商、价格、容量、法域和私有化外部系统没有配置时，统一保持 `not_configured/not_run`，不得由 Mock、合成数据或本地单机结果替代。

`P5-01` 计划基线为 `9088801`，阶段 4 关闭提交和标签为 `add5219`、`stage-4-complete`；原样统一门禁最终以 React `58/58`、Python `835/835`、mypy strict `677` 个源文件及全部工程检查通过。

| 节点 | 交付目标 | 状态 | 验收证据 | Git 提交 |
| --- | --- | --- | --- | --- |
| `P5-01` | 冻结阶段 5 范围、节点依赖、事实源、验收门禁和外部条件 | 已完成 | 2026-08-17：中文计划与报告、专项 `53/53`、阶段 4 关闭事实与外部条件审计通过；原样统一门禁 React `58/58`、Python `835/835`、mypy strict `677` 个源文件及全部工程检查通过，真实供应商、价格、容量、法域和目标环境继续准确保持未配置/未执行 | `9088801` |
| `P5-02` | 建立失败样本、用户反馈和人工修正的版本化质量数据集 | 已完成 | 2026-08-17：冻结 `p5-02-v1` 三来源全合成契约；Revision `20260817_0062` 建立不含正文的不可变样本/数据集/成员事实，完成授权投影、幂等、版本链、并发锁、tombstone、跨空间拒绝和生命周期导出/清除；专项联合 `25/25`、Migration 兼容回归 `11/11`、最终统一门禁 React `58/58`、Python `847/847`、mypy strict `691` 个源文件及全部工程检查通过，真实线上反馈和真实模型质量保持 `not_run` | `f299d1a` |
| `P5-03` | 建立检索、引用、模型、Prompt、工作流和工具分层评估归因 | 已完成 | 2026-08-17：冻结 `p5-03-v1` 全合成六层契约、`deterministic-layered-v1` 评估器、六层样本下限/分数阈值和低基数原因码；实现数据集、服务、AgentRelease、运行配置和评估器身份漂移失败关闭，失败/超时/跳过/样本不足失败关闭，越权/泄漏/无效引用/未授权工具调用硬门禁不可被均分抵消；Revision `20260817_0063` 三张不可变评估事实表，Registry `v11` 导出/清除覆盖，契约 `10/10`、P5-03 PostgreSQL `4/4`、生命周期 `6/6`、全仓统一门禁 React `58/58`、Python `861/861`、mypy strict `700` 个源文件及全部工程检查通过；真实模型质量和线上反馈仍为 `not_run` | `f006c52` |
| `P5-04` | 建立离线评估、灰度观察、反馈回流和发布质量门禁 | 受阻 | 2026-08-17：冻结 `offline`、`canary`、`online_feedback` 三来源和 `offline_release`、`canary_promotion`、`online_continuation` 三阶段门禁，固定引用存在率 `100%`、引用支持率 `>=95%`、回答可接受率 `>=85%`、正向反馈率 `>=80%` 及三类安全事件为零；Revision `20260817_0064` 建立两张不可变运营事实表，Registry `v12` 覆盖导出/清除；契约 `7/7`、P5-04 PostgreSQL `4/4`、Migration/生命周期/发布兼容联合 `27/27`、本地组合 `29/29` 通过。初次统一入口被审批服务拒绝，P5-07 当前树原样统一门禁随后以 React `58/58`、Python `892/892` 和全部工程检查通过；真实供应商与真实质量样本仍为 `not_configured/not_run`，故不得标记完成 | `e821801` |
| `P5-05` | 建立工作空间、服务和 AgentRelease 成本归因 | 已完成 | 2026-08-17：冻结七类组件、逐 Attempt 多计量项、整数金额与逐条向上取整，保存平台估算、供应商报告、最终确认金额和来源；Revision `20260817_0065` 建立窗口、账本、聚合三张不可变事实表，Registry `v13` 覆盖隔离导出/清除；契约 `7/7`、PostgreSQL `4/4`、生命周期/Migration/P5-05 `15/15`、全部受影响回归 `43/43` 通过。初次统一入口被审批服务拒绝，P5-07 当前树原样统一门禁随后以 React `58/58`、Python `892/892`、mypy strict `729` 个源文件及全部工程检查通过，工程阻塞解除；真实价格与供应商账单继续保持 `not_configured` | `eaa58fa` |
| `P5-06` | 建立预算、异常用量、成本预测和自动降级 | 待开始 | 等待 `P5-05` 与真实价格 | 待提交 |
| `P5-07` | 建立 L1～L4 隔离等级策略、资格和迁移治理 | 已完成 | 2026-08-17：冻结 `p5-07-v1` 四级隔离、套餐上限、合规状态、活动迁移状态和路由规则；Revision `20260817_0066` 建立不可变策略/路由、受约束迁移计划和数据库失败关闭，Registry `v14` 保留治理历史；个人越级、套餐漂移、跨空间、非法转换、迁移删除、恢复期间第二计划、错误 L2 命名空间及 P5-08/P5-10 前 L3/L4 路由均被拒绝。契约、PostgreSQL、生命周期和 Migration 联合 `20/20`、受影响旧 head 回归 `21/21`、统一门禁 React `58/58`、Python `892/892`、mypy strict `729` 个源文件及全部工程检查通过；真实 L3/L4 环境保持 `not_configured` | 待提交 |
| `P5-08` | 建立 L3 数据库、对象桶和密钥隔离迁移恢复 | 进行中 | `P5-07` 已完成；从合成高合规工作空间的资源档案、复制校验、单写切换、回滚和恢复开始实施 | 待提交 |
| `P5-09` | 建立法规策略、法律保留、解除和合规证明 | 待开始 | 等待 `P5-07` 与法域配置 | 待提交 |
| `P5-10` | 建立 L4 私有化/独立实例交付、升级与恢复 | 待开始 | 等待 `P5-08`、`P5-09` | 待提交 |
| `P5-11` | 审计服务拆分和 Go 演进量化触发条件 | 待开始 | 等待质量、成本和私有化事实 | 待提交 |
| `P5-12` | 建设质量、成本、隔离和合规运营控制台 | 待开始 | 等待运营与隔离能力 | 待提交 |
| `P5-13` | 完成联合验收、ReleaseManifest、阶段关闭提交和标签 | 待开始 | 等待全部阶段 5 节点 | 待提交 |

## 9. 当前限制

- 当前没有真实模型供应商配置，`provider_integration` 和 `ai_quality` 暂不判定通过。
- P5-07 当前树已原样通过 `./scripts/verify`，此前 P5-04/P5-05 的审批服务 `502 Bad Gateway` 工程阻塞已解除；P5-04 仍因已审核真实供应商、固定模型/参数/网络区域和足够授权样本缺失而保持受阻。
- P5-05 机制节点已完成，但真实价格与供应商账单继续为 `not_configured`，因此 P5-06 的真实价格依赖尚未满足。
- 本地开发实例已经具备仅限 `local/test` 的固定内置 Mock Provider 和当前运行配置自举；该能力只用于零配置功能闭环，不代表真实供应商兼容性、质量、成本或数据政策验收。
- 当前没有容量压测机，`capacity_certification=not_run`。
- 本节点仅以 `AI_PLATFORM_MIN_FREE_DISK_GB=40` 做进程级小规模运行验收；默认 50 GB 安全基线未修改，该覆盖不构成容量认证。
- 当前未获镜像元数据外发授权，镜像漏洞扫描为 `not_configured`；Linux 宿主机验收为 `not_run`，两者阻断正式发布但不阻断本地功能建设。
- 上述限制不改变阶段 3、阶段 4 的本地通过结论，也不阻塞阶段 5 使用合成数据和本地依赖建设确定性治理机制，但继续阻断真实供应商质量、真实价格、真实外部工具、客户环境、生产容量和正式发布结论。

## 10. 更新规则

节点状态、验收证据和提交信息按 [交付、文档与 Git 管理规范](./governance/delivery-and-git.md) 更新。每次节点提交完成后，本看板必须回填短 SHA；为了避免用修改历史的方式伪造追溯，SHA 回填可随下一个节点提交进入 Git，阶段关闭前必须全部核对。
