# Agent 控制面合成数据

`p3-01-v1.json` 是阶段 3 的版本化全合成验收基线，覆盖个人所有者审批、企业多级审批、草稿执行拒绝、测试门禁、审批摘要绑定、跨空间隔离、发布不可变、控制面故障、灰度、回滚、并发发布、菜单接口统一授权、只读工具和 Run 版本绑定。

本目录不保存真实客户、Prompt、文档、API Key、模型凭证或供应商响应。`P3-02`～`P3-12` 分节点把场景映射为可执行测试，`P3-13` 再形成同一次阶段联合验收清单；未实现的场景只能表示冻结要求，不能标记为通过。
